[package]
name = "neutrino-http"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.7"
tower-http = { version = "0.6.6", features = ["trace"] }

rand.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true

neutrino-common = { path = "../neutrino-common" }
neutrino-sqlite = { path = "../neutrino-sqlite" }
