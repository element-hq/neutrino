## endpoints

### Client-Server API

- POST /_matrix/client/v3/createRoom 
- PUT /_matrix/client/v3/rooms/{roomId}/send/{eventType}/{txnId} 
- PUT /_matrix/client/v3/rooms/{roomId}/state/{eventType}/{stateKey}
- POST /_matrix/client/unstable/org.matrix.simplified_msc3575/sync (only supporting room events & state events)
- GET /_matrix/client/v3/rooms/{roomId}/members 
- GET /_matrix/client/v3/rooms/{roomId}/messages 
- GET /_matrix/client/v3/rooms/{roomId}/event/{eventId}
- GET /_matrix/client/v3/rooms/{roomId}/state 
- GET /_matrix/client/v3/rooms/{roomId}/state/{eventType}/{stateKey}
- POST /_matrix/client/v3/rooms/{roomId}/invite 
- POST /_matrix/client/v3/rooms/{roomId}/leave

### Server-Server API

- PUT /_matrix/federation/v1/send/{txnId}
- GET /_matrix/federation/v1/backfill/{roomId}
- POST /_matrix/federation/v1/get_missing_events/{roomId}
- GET /_matrix/federation/v1/event/{eventId}
- GET /_matrix/federation/v1/make_join/{roomId}/{userId}
- PUT /_matrix/federation/v2/send_join/{roomId}/{eventId} 
- PUT /_matrix/federation/v2/invite/{roomId}/{eventId}
- GET /_matrix/federation/v1/make_leave/{roomId}/{userId}
- PUT /_matrix/federation/v2/send_leave/{roomId}/{eventId}


## status
- [x] project scaffold (Cargo.toml, main.rs, module structure)
- [x] Write a .github/workflows/ci.yml to run cargo tests, complement, clippy, formatting, compile.sh script for uniffi, and to fail if any fail. Run linting in parallel.
- [x] StorageBackend trait defined
    - multiple developers will be relying on this not changing (one to implement the interface, the other using it). If it later turns out that the trait needs to change, prompt and add decision log immediately.
- [x] SQLite storage backend implementation
    - lives in the `neutrino-store-sqlite` crate (`SqliteStore`). The live router and the e2e HTTP tests run against `SqliteStore::open_in_memory()`; the in-process `InMemoryStore` is retained only as the seeding helper for sliding-sync unit tests.
- [ ] Room state machine (v12 auth + state-res v2.1, MSC4242)
    - [x] Phase 0: types (`neutrino-state` crate: `RoomVersion`, `Event`, `StateMap`, `FormatError`, `AuthError`, `CoreError`)
    - [x] Phase 1a: format validation (`validate::parse_event`, 40 tests)
    - [x] Phase 1b: reference validation (`validate::validate_references`, 10 tests). Introduced a minimal `StateProvider` trait (one method, `get_event`) ahead of Phase 5 so 1b could land independently.
    - [x] Phase 2: auth events selection (`auth_events::auth_event_keys` + `calculate_auth_events`, 15 tests)
    - [x] Phase 3: auth rules v12 (`auth_rules::check_auth_rules`, 24 tests; rules 4–11 in spec order)
    - [x] Phase 4a: `separate` + `conflicted_subgraph` + `auth_chain_difference` (`state_res` module, 13 tests). Also landed the public `InMemoryStateProvider` (Phase 5) here since the provider gained `auth_event_ids` and 4a wanted to test against a real impl.
    - [x] Phase 4b: reverse-topo power sort + IAC pass 1 (`power_of_sender` + `reverse_topological_power_sort` + `iterative_auth_checks` in `state_res.rs`, 19 tests). `AuthContext::new` + `user_power` promoted to `pub(crate)` so creator-set + PL parsing stay defined once.
    - [x] Phase 4c: mainline + IAC pass 2 + unconflicted merge → `resolve_state` (`is_power_event` / `split_power_events` / `mainline` / `mainline_position` / `mainline_sort` / `resolve_state` in `state_res.rs`, 25 case tests + 2 props). End-to-end state-res v2.1 is now in.
    - [x] Phase 5: in-memory `StateProvider` (landed alongside Phase 4a)
    - [x] Phase 6a: `state_at_heads(heads, provider, cache)` + `state_before(event_id, provider)` in `state_res.rs`. `state_at_heads` is the general "state at the merge of these heads" primitive (== state-before an imaginary next event whose `prev_state_events` are `heads`); `state_before` is the thin wrapper that looks up the event and delegates. `StateBeforeCache` is a public alias for the memoisation table, threaded by callers (notably `RoomCore::apply`) across back-to-back walks.
    - [x] Phase 6c: `RoomCore::apply` in `crates/neutrino-state/src/room_core.rs`. `Effect { Persist { event, soft_failed }, UpdateCurrentState }`. Orchestration: room_id-mismatch guard → FE-already-present idempotent return → validate_references → state_at_heads(prev_state_events) → check_auth_rules → (state event) update FEs + recompute current_state via state_at_heads(new_fes) with shared cache → (non-state event) soft-fail check sets `Persist.soft_failed`. 11 case tests covering happy path, hard rejects, foreign-room rejection, idempotent re-apply, auth-rule integration (rules 4 / 5.4 / 5.5 / 5.6 / 8 / 10.4).
    - [x] Phase 6b: `SqliteStateProvider<'a>` in `crates/neutrino-store-sqlite/src/store/state_provider.rs`. Holds `&'a rusqlite::Connection`; callers construct inside a `run_read` / `run_write` closure on the blocking thread. `get_event` is a single SELECT hydrating via `EventRow`; `auth_chain` is one recursive CTE over `event_edges WHERE edge_type='auth'` with a `LEFT JOIN events` for strict-closure miss detection (any unresolved id → `StateResError::MissingEvent`). 9 unit tests covering hit / miss / rejected-roundtrip, empty seeds, single seed, linear chain, diamond ancestor dedup, missing seed, dangling parent edge. Schema amended in place: `events.rejected INTEGER NOT NULL DEFAULT 0 CHECK (rejected IN (0,1))`; `user_version` stays at 1 (no live data, no migration framework yet). `rejected: bool` lifted onto `neutrino_common::Event` itself (same "server-computed, co-located on struct, not in `raw`" pattern as `auth_events`); `EventInfo` wrapper deleted; `StateProvider::get_event` now returns `Option<Arc<Event>>` directly. `StateResError` grew an `Internal(String)` variant so SQL/driver faults can surface without panic. Storage-side `Effect` processor (apply + atomic persist) remains the next task — out of scope for 6b.
    - [x] Phase 6d: storage-side apply+persist via a per-room actor (single `RoomCore` owner, serialised applies). Design settled 2026-05-29 (see decisions log). CSAPI writes now flow through the actor. Split into PRs:
        - [x] PR1: `RoomCore` tracks timeline forward extremities (`forward_extremities`) alongside the existing state-DAG heads; `apply` advances them for every accepted event **except soft-failed ones** (state + non-state), and the idempotency guard now checks both head-sets. `rooms` schema gains `forward_extremities` + `state_dag_forward_extremities` JSON columns (`DEFAULT '[]'`, no version bump). 5 new `room_core` tests. No actor / persist yet. Review fix: soft-failed events are persisted but never become forward extremities (synapse#5269/#5274) — see decisions log.
        - [x] PR2: storage⇄`RoomCore` bridge (reordered ahead of the actor — the actor depends on it and this layer is unit-testable standalone). `RoomCore::hydrate(room_id, timeline_fes, state_fes, current_state)` constructor (neutrino-state). Two trait methods (decision A: on the trait; only `SqliteStore` impls `StorageBackend`, so no other impl rippled): `RoomStore::forward_extremities(room_id) -> Option<(timeline, state)>` (load half, parses the two JSON columns) + `EventStore::persist_resolved_event(event, timeline_fes, state_fes, current_state_delta)` (persist half — one write txn: event+edges (no implicit current_state upsert), apply the current-state delta, replace the two `rooms` FE columns, fire the watch; no outbox). **current_state is delta-driven** (`Effect::UpdateCurrentState(StateDelta)`, `StateDelta = BTreeMap<(type,state_key), Option<OwnedEventId>>`): `apply` diffs old vs recomputed state → `Some(id)` set / `None` remove; the persist layer upserts each set key by deriving event_type/state_key/membership from the already-persisted `events` row via `INSERT…SELECT json_extract` (0 rows ⇒ the id isn't persisted ⇒ `Internal`, the asserted pre-condition), and deletes each removed key. Handles the federation multi-key/removal case, not just the local single-key one. **`soft_failed` now persisted**: `events.soft_failed` column (`DEFAULT 0`, no version bump) + `Event.soft_failed` field (co-located server verdict, same pattern as `rejected`); `apply` stamps it on the event (dropped from `Effect::Persist`). 9 new tests (hydrate round-trip; FE-load None/empty; persist state/non-state; delta None-removes; delta unpersisted-target errors; soft_failed round-trip).
        - [x] PR3: the per-room actor + registry in **neutrino-http** (not neutrino-main — that lacks the neutrino-state/sqlite deps; neutrino-http's AppState already holds `Arc<SqliteStore>`). `RoomRegistry { RoomId → mpsc::Sender<Command> }` lazily bootstraps a room's actor on first use (`forward_extremities` + `current_room_state` → `RoomCore::hydrate`), double-spawn-guarded. The actor owns the `RoomCore`, serialises `Command::Send`: builds the event off the current heads (prev_events/prev_state_events) with `auth_events = calculate_auth_events(_, current_state)`, applies to a **clone**, and on accept forwards the `Effect::Persist` event + `Effect::UpdateCurrentState` delta + the post-apply head-sets to `persist_resolved_event`, adopting the clone only after the commit. `RoomCore::apply` needs a sync provider that's `pub(crate)`-bound, so a new `pub SqliteStore::apply_event(room, event) -> (RoomCore, Result<Vec<Effect>, CoreError>)` bridges it (runs apply on a reader conn). `create_room` now seeds the FE columns to the last-written event (createRoom emits a linear state chain ⇒ that's the sole head of both DAGs), so the actor can bootstrap a freshly created room. 3 direct-drive tests (state event advances heads; message persists without state change; unauthorized event rejected, state untouched). Module is `#[allow(dead_code)]` until PR4 wires it. No HTTP/CSAPI wiring yet.
        - [x] PR4: CSAPI writes wired through the actor. `App` gains `room_registry: Arc<RoomRegistry>`. `put_event` (`/send/{type}/{txn}`) rerouted through `registry.send_event(.., None, ..)` — replaces the placeholder that built **empty `prev_events`** (it was explicitly deferred to Phase 6); messages are now DAG-linked + soft-fail-checked. New `/state/{type}/{stateKey}` + `/state/{type}` (empty-key) endpoints route through `send_event(.., Some(key), ..)`. `sender = config.user_id()` (auth stubbed). `RoomActorError` → HTTP: UnknownRoom→404, Build→400, Apply→403 M_FORBIDDEN, else→500. createRoom stays **direct** (`store.create_room`, atomic — emits create + creator-join + optional name); the actor bootstraps lazily on the first send after, so create-batch atomicity is preserved (not routed through the actor). Existing message-send e2e tests pass unchanged (createRoom via endpoint ⇒ creator joined ⇒ sends auth-pass); 1 new e2e test for `/state`. Remaining follow-ons (not 6d): client-timeline filtering on `soft_failed`; outbox/federation delivery; createRoom-through-actor if its batch ever needs state-res.
- [x] Client-Server Sliding Sync MSC4186 implementation
    - wired into the live axum router at `POST /_matrix/client/unstable/org.matrix.simplified_msc3575/sync`
    - 51 unit tests (one ignored — state-removal not synthesizable via the trait) + 9 end-to-end HTTP tests; every test owns its own `SqliteStore` on a `tempfile::NamedTempFile`.
    - filters, lazy members, heroes, `expanded_timeline`, state-stub emission are intentional gaps — see MSC4186-gaps.md; kick/ban/leave handling is implemented via `rooms_with_membership` (PLAN.md 2026-05-18 entry).
- [x] Client-Server write endpoints (PUT/POST) implementation
    - `/_matrix/client/v3/createRoom` and `/_matrix/client/v3/rooms/{room}/send/{type}/{txn}` route through `RoomStore::create_room` and `EventStore::persist_event` on the active `StorageBackend`. Storage errors surface as 500 `M_UNKNOWN` rather than being silently dropped. Membership tracking is automatic from `m.room.member` events.
- [x] Client-Server legacy `/sync` → MSC4186 sliding-sync stub
    - `GET /_matrix/client/v3/sync` lives in `crates/neutrino-http/src/legacy_sync/`. Pure translation helpers (`parse_legacy_query`, `synthesize_v5_request`, `translate_response`) compose with the existing `sliding_sync::handle` — no changes to sliding-sync internals. MSC4222 `state_after` dual-emitted alongside legacy `state`; `/versions` advertises `org.matrix.msc4222`. Knock rooms emit under `rooms.knock` with manually-stripped `knock_state.events` (ruma has no `Raw<AnySyncStateEvent>` → `Raw<AnyStrippedStateEvent>` cast). 32 unit tests + 6 e2e tests; each e2e builds a fresh `router(config)` whose `AppState::new` opens a file-backed `SqliteStore` on a `tempfile::NamedTempFile` (same pattern as `tests/e2e_sliding_sync.rs`). See `docs/legacy-sync-stub.md`.
- [x] CSAPI event delivery enriches `event_id` (and `room_id` for create events)
    - `neutrino-common::event_view` houses `From<&Event> for Raw<AnySyncTimelineEvent>`, `From<&Event> for Raw<AnyTimelineEvent>`, `TryFrom<&Event> for Raw<AnySyncStateEvent>`, `TryFrom<&Event> for Raw<AnyStrippedStateEvent>`, and the infallible free `strip_state_event(&Raw<AnySyncStateEvent>) -> Raw<AnyStrippedStateEvent>` (orphan rule forbids it as a trait impl). Both `sliding_sync/build.rs` and `legacy_sync/translate.rs` migrated; bespoke `strip_state_event` helpers in those modules deleted. See `docs/event-view-conversions.md`. 13 unit tests + 3 e2e regressions that would have caught the original complement failure.
- [x] Testing-only multi-user identity shim (`--features multi-user-shim`)
    - `multi_user.rs`: `UserTokens` map, `mint_token`, `resolve`, `provision`. `AuthUser` extractor: 401 `M_MISSING_TOKEN` / `M_UNKNOWN_TOKEN` with shim ON; single-user passthrough OFF. `/register` (two-step UIA) + `/login` mint per-user `syt_<random>` tokens. `createRoom`, `/send`, `/state`, sliding-sync + legacy-sync all accept `AuthUser` and attribute events to the token's user. 4 e2e tests in `tests/e2e_multi_user.rs` (distinct tokens, per-user room isolation, 401 missing, 401 unknown); file compiles to 0 tests feature-OFF.
- [x] Client-Server membership endpoints (`/join`, `/leave`, `/invite`, `/kick`, `/ban`, `/unban`, global `/join/{roomIdOrAlias}`)
    - `crates/neutrino-http/src/membership.rs`: six thin handlers emit `m.room.member` state events through the existing `RoomRegistry::send_event` actor path — auth (v12 rule 5), state-res, and persistence are unchanged. `/join`/`/leave` target the caller; `/invite`/`/kick`/`/ban`/`/unban` target `body.user_id`; `/kick` and `/unban` both emit `membership: leave`. `/unban` first pre-checks the target is currently `ban` (else 403 `M_BAD_STATE`, matching Synapse) so it can't be abused to kick a joined member, and propagates `reason`; `/leave` is a spec no-op (200) when the caller isn't `invite`/`join`/`knock` in an existing room. Both `/leave` and `/unban` return 404 `M_NOT_FOUND` ("Not a known room") for a room that was never created — mirroring Synapse, which 404s a leave/unban (unban is internally a leave) before any membership-state check (`room_member.py:1135-1152`). `/createRoom` now emits a creator-authored invite per `invite[]` entry (`is_direct` propagated). The global `POST /_matrix/client/v3/join/{roomIdOrAlias}` (the endpoint Complement's `MustJoinRoom` actually uses — the room-scoped `/rooms/{id}/join` is never hit by Complement) delegates straight to `membership::join`; it resolves room *ids* only (an alias `#…` → 400 `M_INVALID_PARAM`, alias resolution unimplemented) and ignores the `server_name` query. 14 e2e tests in `tests/e2e_multi_user.rs` (feature `multi-user-shim`). Not in scope: `/forget`, alias *resolution*, displayname/avatar carry-over, remote-user delivery; Synapse/Complement test ports deferred to a follow-up PR.
- [ ] Server-Server /send implementation
    - MUST handle retrying on restart, MUST NOT lose events. Events MUST eventually be sent over federation.
    - [x] `RoomCore::apply_pdu` — the single integration path for both local sends (build → apply_pdu) and received federation PDUs. Replaces the old `apply`. Persisted-check idempotency; computes `auth_events` itself (MSC4242 — sole authority, `build_local_event` no longer sets them); conditional `UpdateCurrentState` (a state event that wins auth but loses state-res emits `Persist` alone); three-way disposition (DROP=Err malformed/misrouted, RETRY=Err missing-ancestry/fault via `CoreError::is_retryable`, REJECT=`Ok` with `Event.rejected`). Actor: `RoomRegistry::apply_pdu` (`Command::ApplyPdu`) persists rejects (federation policy); local `Command::Send` discards a reject as 403 (`RoomActorError::Rejected`). Fixed a v12 state-res conflict-resolution bug found while testing this (create excluded from auth_events but auth needed it — see decisions log); the conflict test now passes.
    - [x] Inbound `PUT /_matrix/federation/v1/send/{txnId}` — `crates/neutrino-http/src/federation/send.rs` + 9 e2e tests in `src/federation/tests.rs`. Hand-rolled `TransactionBody` (no ruma `federation-api` — that feature on ruma 0.15.1 pulls an unpublished sub-crate; mirrors the wire-verbatim pattern in `backfill.rs`/`get_missing_events.rs`). Flow: 400 on malformed body / >50 PDUs; txn dedup via `FederationInbox::record_federation_txn(origin, txn_id)` (duplicate → empty `{pdus:{}}` 200); EDUs deserialized-and-dropped (stub); each PDU parsed via `event_id::from_wire` (unkeyable failures silently skipped); intra-batch Kahn toposort over `prev_events ∪ prev_state_events`; apply each through `RoomRegistry::apply_pdu`; per-PDU spec result map `{pdus:{$id:{} | {error}}}` (a persisted reject is `{}`, not an error — federation policy). Gap-fill loop terminates on (1) `apply_pdu` Ok, (2) fetcher no-progress, (3) `MAX_GAPFILL_ROUNDS`. No X-Matrix auth (trusted mesh). **Outbound `/get_missing_events` fetch is behind a `MissingEventsFetcher` trait wired to a `NoFetcher` no-op** — the real reqwest-backed impl + server-name resolution land with outbound delivery; until then a retryable PDU resolves to terminal condition 2. So a txn whose PDUs arrive with complete ancestry (the common in-order case) works end-to-end today.
    - [ ] Outbound delivery (the "retry on restart / never lose / eventually send" clause). PR breakdown agreed 2026-06-03 (see decisions log): PR1 outbox population (DONE), PR2 reqwest `FederationClient` + resolver, PR3 per-destination sender pool + startup wiring in `neutrino-http::serve` (not neutrino-main — lacks the deps), PR4 reqwest `MissingEventsFetcher` replacing `NoFetcher`. Lands before the inbound stage-then-async redesign (shares the reqwest client + restart-wiring).
        - [x] PR1: outbox population. `EventStore::persist_resolved_event` gains `destinations: &[&ServerName]`, written one `outbox` row each **inside the existing persist txn** (atomic — a crash can't persist the event but drop its delivery); shared `insert_outbox_rows` helper now backs both `persist_event` and `persist_resolved_event`. The actor (`room_actor.rs`) computes destinations from the **post-apply** `current_state` via `outbound_destinations` = join-member servers + (for `m.room.member`) the target's server (so leave/kick/ban reaches the departing server, mirroring Synapse "hosts in room + target"), minus our own server; `RoomRegistry`/`RoomActor` gained `own_server` (from `config.server_name`). Local `Command::Send` enqueues; `Command::ApplyPdu` passes `&[]` (a received PDU is not re-originated). No sender yet — outbox just accumulates. 2 sqlite tests (per-dest rows via `pending_pdus`; empty-slice writes none) + 2 actor tests (local send → remote member gets it, self never does; kick of a remote member still federates to their server).
    - [ ] **Inbound `/send` redesign → stage-then-async-process (FUTURE; design agreed 2026-06-03, see decisions log).** The current handler processes PDUs *synchronously* inside the HTTP request (parse → toposort → apply → gap-fill loop), which (a) blocks the response on auth + (future) peer backfill round-trips, and (b) only durably "accepts" a PDU once it's fully applied — so a retryable/failed PDU can be lost. RoomCore's persisted-check already makes re-application idempotent, so `/send` doesn't need to fully process to be safe; it needs to *durably accept* and not wedge. Target shape:
        - **Sync path:** validate envelope → `from_wire` each PDU (cheap, no I/O) → write to a **staging table** keyed by event_id (raw bytes verbatim — event_id is computed from them; compute the id sync for the dedup key) → return 200. Malformed-envelope 400s and unkeyable-PDU drops stay synchronous; the per-PDU result map becomes optimistic/empty (spec-fine: `error` is optional and senders ignore it).
        - **Background path:** a worker drains staging grouped by room_id and feeds the existing per-room actor (`Command::ApplyPdu` — it already serialises per room): toposort → apply → gap-fill/backfill. On restart, neutrino-main enumerates unprocessed staged PDUs grouped by room and replays them (symmetric with the outbox startup wiring). The gap-fill loop **moves here from the request path** — BUG-2 plus the forward-looking findings (fault-vs-no-progress, blocking-on-backfill) dissolve into background retry-with-backoff.
        - **Wedge avoidance:** a PDU that errors is logged + attempt-counted / marked-failed so it neither blocks its room's queue nor retries forever; a retryable gap is re-attempted with backoff (this is where "events MUST eventually be processed" lives).
        - **Keep the txn_id dedup** (`FederationInbox::record_federation_txn`) as a cheap "whole txn already staged" short-circuit (Kegan, 2026-06-03) — alongside, not replaced by, the per-event_id staging dedup.
        - **Needs a StorageBackend trait change** (a staging sub-trait, e.g. `stage_pdu(origin, room_id, event_id, raw)`, `pending_staged()`, `mark_staged_processed(event_id)`) — get sign-off + decision-log entry per CLAUDE.md before implementing. Consider doing **outbound delivery first**: it shares the restart-wiring + reqwest-fetcher pieces this can then reuse.
        - **BUG-1 (fixed 2026-06-03):** `toposort` now dedups by event_id before the indegree graph (a repeated PDU with an in-batch child underflowed a `usize` → debug panic). Regression test `send_handles_duplicate_pdu_in_batch`.
- [ ] Server-Server invite/join/leave implementation
- [x] Server-Server backfill/get_missing_events implementation
    - MUST incrementally persist progress when filling in the state DAG
    - [x] `POST /_matrix/federation/v1/get_missing_events/{roomId}` — `crates/neutrino-http/src/federation/` + `src/federation/tests.rs` (12 tests). Wire-bytes-verbatim response; `min_depth` parsed-and-ignored; trusted-mesh skips X-Matrix auth and history-visibility filtering. Outbound `/get_missing_events` (we call peers to fill our own gaps) is state-res-blocked, Phase 6 territory.
    - [x] `GET /_matrix/federation/v1/backfill/{roomId}` — `crates/neutrino-http/src/federation/backfill.rs` + 8 e2e tests in `src/federation/tests.rs` (BF1–BF8). Reuses `DagStore::events_before` (no trait change); seeds-included, newest-first; wire-bytes-verbatim `{origin, origin_server_ts, pdus}` envelope; unknown `v` pre-filtered via `get_events` so it skips rather than 500s; hand-rolled `RawQuery` parser for repeated `?v=`.

All status points MUST have tests.

## in progress

(none — SQLite backend complete; sliding-sync and CSAPI write endpoints both run against `SqliteStore` end-to-end.)

## test plan: RoomCore fork/merge proptests (PLANNED — not yet implemented)

Goal: exercise `RoomCore::apply_pdu` over randomly-generated state DAGs containing
real forks (sibling events off one head) and merges (an event whose
`prev_state_events` references ≥2 heads). Predominantly state events; occasional
message events to exercise timeline-vs-state head divergence. Lives in
`crates/neutrino-state/tests/prop.rs` (extends the existing topo-order strategies).

### Properties

- **P1 — order-independence (flagship). [DONE, creator-only]** One generated DAG,
  applied to N fresh `RoomCore`s in N random *topological* orders, must yield
  identical `current_state` + both head-sets. The defining property of state-res;
  forks and merges fall out of the DAG shape for free. Topo-order is mandatory anyway
  — `apply_pdu` returns retryable-`Err` when ancestry is absent. Implemented as
  `creator_only_fork_merge_order_independent`.
- **P2 — head-set bookkeeping (free co-check on P1's DAG). [DONE, creator-only]**
  Recompute from raw DAG structure: timeline FEs = events referenced by no applied
  `prev_events`, minus soft-failed; state FEs = state events referenced by no
  *state event's* `prev_state_events`. Assert == RoomCore's tracked sets. Independent
  of state-res (covers synapse#5269 soft-fail-extremity). Implemented as the
  `expected_heads` oracle, asserted inside `creator_only_fork_merge_dag_accepts_every_event`
  (oracle is order-independent, so build-order + P1 establishes it for all orders).
  **Why it mattered:** P1 only proves the
  head-sets are *consistent* across orders, never that they're *correct* — a
  systematically-wrong-but-deterministic head-tracking bug sails through P1. P2 is
  the independent structural oracle that closes that gap, and it rides the existing
  `Dag` with zero new generator work (the soft-failed clause is vacuous on the
  creator-only profile but the core "FE = unreferenced event" is exercised by every
  fork/merge/message). Foundation for everything downstream.
- **P3 — idempotency. [DONE 2026-06-03 — on the adversarial generator]** After
  applying the DAG, replay any random subset in any order → current_state +
  head-sets unchanged (federation re-send reality). Originally scoped to ride
  P1's creator-only DAG, but that generator was retired in the 2026-06-03
  consolidation, so it landed on `build_adv_dag` instead (a strict superset —
  the replay subset now exercises rejects + soft-fails too, all of which are
  persisted). Implemented as `adv_replay_is_idempotent`: full apply in build
  order, snapshot, then re-apply an arbitrary index multiset; each re-apply
  must return empty effects (`apply_pdu`'s persisted-check, `room_core.rs:330`,
  returns `Ok(vec![])` before any mutation) and the final state + both head-sets
  must equal the snapshot.
- **P4 — rejection (separate adversarial generator). [DONE 2026-06-02]**
  Rejected-verdict is order-independent; rejection cascades through
  `prev_state_events`; a rejected event never appears in `current_state`. The
  multi-user shadow-auth generator (`build_adv_dag`) and all three properties
  landed: `adv_order_independent_invariants` (order-invariance of
  `current_state` + `state_fes` + reject-projection — rejection determinism is
  the reject-projection clause) and `adv_rejection_cascade_and_isolation`. NOTE:
  soft-fail verdicts are NOT order-independent (checked against live
  `current_state`, `room_core.rs:433`) — only reject is, which is why
  order-independence covers {`current_state`, `state_fes`, reject-verdicts} and
  the verdict is projected to rejected-or-not before comparison.
- **P5 — message-event invariants. [DONE, creator-only]** A non-state event never
  touches `current_state` or state FEs, only timeline FEs; timeline-vs-state FE
  divergence ⊆ {messages, soft-failed}. The creator-only generator already emits
  messages (`DagOp::Message`), so this is another free co-check; pairs naturally with
  P2. Implemented as a block inside `creator_only_fork_merge_dag_accepts_every_event`
  (rides P2's `outcome` + `expected_heads`, no extra build/apply). Asserted as the
  *one-directional* `timeline_fes \ state_fes ⊆ {messages}` — the reverse direction
  (a state FE that is not a timeline FE) is expected and is *not* a message: a state
  head a later message named in `prev_events` stays a state head but drops out of the
  timeline heads. The "soft-failed" clause is vacuous on the creator-only profile.

The "incremental == batch" idea (current_state == fresh `state_at_heads(state FEs)`)
is deliberately NOT a property here: it's tautological against `RoomCore`, which sets
`current_state = state_at_heads(new_fes)` on every state apply (`room_core.rs:399`).
The meaningful part — that the emitted `StateDelta` stream telescopes to the resolved
`current_state` — is already asserted inside `apply_dag` (folded deltas ==
`room.current_state()`). An independent batch-vs-delta check only has signal at the
sqlite persist layer (current_state built purely from deltas, never recomputed); if
wanted, it belongs in a store-sqlite test, not these RoomCore proptests.

P1+P2+P5 rode the (now-retired) creator-only DAG; P4 + P3 ride the adversarial
profile. **All properties P1–P5 have landed** (P1/P2/P5's creator-only invariants
were folded into the adversarial suite or dropped in the 2026-06-03
consolidation; see the decisions log).

### Generator design

Model-based, intent-tagged, topo-order. The recipe is a flat `Vec<Op>` (shrinkable;
proptest shrinks the recipe, not raw events), realised left-to-right against a
**shadow room model**.

- **Shadow model = auth-relevant projection only**, NOT full state-res: `members:
  Map<UserId,Membership>`, `power_levels` (users + action levels + defaults),
  `join_rule`, creator. Maintained *per head* so divergent branches carry divergent
  state. KEY PRINCIPLE: the shadow model is a *coverage* device, not a *correctness*
  device — P1/P2/P3 hold regardless of whether events are accepted or rejected, so
  the model only needs to be good enough to keep the *accepted-event yield* high.
  It may merge branch states approximately (e.g. last-writer-by-ts); a wrong guess
  just produces an extra reject, which P4 absorbs.
  - **Mirror the real drop-rejected-from-heads rule.** `apply_pdu` drops a rejected
    event from *both* head-sets (returns at `room_core.rs:375` before any head
    advance), so a head-following generator never references a reject in
    `prev_state_events` — the prev-state-reference cascade does NOT arise naturally;
    it is opt-in (deliberately wiring a child to a known reject, for P4). The shadow
    model must therefore *also* drop predicted-rejects from its heads and apply
    post-reject state. The cascade that DOES compound naturally is **semantic**: a
    rejected `m.room.member` join leaves the actor un-joined, so its later events
    fail rule-5 regardless of references. Mirroring the real accept/reject + drop
    keeps rejection rate ≈ the deliberate-`Unauthorised` rate; runaway only appears
    on shadow/real disagreement (i.e. desync — bounded by size, below).
- **Determinism & timestamps.** Build the DAG **once** and reuse the same `Event`
  objects across all N application orders (P1) — do NOT rebuild per ordering.
  Timestamps are a pure function of topo position (`base + topo_index`), NOT a global
  `AtomicU64` counter: that counter leaks state across proptest cases, so shrink/
  replay yields a different ts and P1 silently breaks. Monotonic, real-time-free,
  reproducible.
- **Bounded size is the primary desync control.** Desync is injected only at *merges*
  (linear runs are exact) ∝ #merges × conflict depth, so shallow DAGs barely drift.
  A fork+merge needs only ~5–6 events. Bound total events (~10–20) and fork-width
  (~2–3). Bonus: proptest shrinks small DAGs into intelligible counterexamples; large
  ones are unreadable. This largely subsumes any need for desync-recovery tricks.
  (Optional yield tweak, not load-bearing: a self-leave is unconditionally auth-valid
  and overwrites the membership key, so it resyncs *that one key* — but not PL/others,
  and desync only costs yield not validity, so don't build architecture around it.)
- **Bounded user pool** (alice=creator + bob/carol/dave), sampled via
  `proptest::sample::select`. Small pool is essential: fresh uuids would make every
  event touch a distinct state key → zero conflicts → merge math never exercised.
  A dense pool forces invite→join→kick→re-join, repeated PL targets, same-key
  conflicts.
- **Op = (head-selection, intent).** head-selection picks a subset of current heads
  (1 = extend, the same head twice across ops = fork, ≥2 in one op = merge). Intents:
  `Join | Invite | Leave | Kick | Ban | Unban | Topic | Name | PLPromote | PLDemote
  | Message | Unauthorised(kind)`. The generator uses the shadow state-before at the
  chosen heads to *realise* the intent (pick a valid sender/target), or for
  `Unauthorised` deliberately pick a sender/target the model says fails (powerless
  sender sets topic; uninvited user joins invite-room; kick a higher-power user).
- **Explicit conflict-fork primitive.** Random head-selection rarely produces a
  *conflicting* fork. Add an op that, off one head, spawns two branches each mutating
  the *same* key (same membership target, or same PL `users` entry), then a merge op
  referencing both — this guarantees the mainline-sort / IAC path is hit. PL
  conflicts are the richest merge case (two concurrent promotions/demotions of one
  user resolved by mainline ordering).
- **PL promotions/demotions.** Promote: raise a joined user below a level (valid iff
  sender outranks the new level). Demote: lower a user (rule 10 forbids demoting at/
  above your own level → generate both the creator-demotes-lesser valid case and the
  peer-demotes-peer invalid case).
- **Coverage assertions. [DONE for the creator-only buckets]** A separate
  non-shrinking corpus test must confirm the generator actually produced: a merge
  resolving a PL conflict, a rejected event with a dependent child, a soft-failed
  message, a multi-head merge. Otherwise the rejection / conflict-merge coverage (P4,
  P5) passes vacuously on linear all-accepted chains. Implemented as
  `fork_merge_generator_coverage_corpus` — a plain `#[test]` that drives `dag_op()`
  `2000`× against a `TestRunner::deterministic()` runner, classifies each DAG, and
  asserts floor *fractions* (not ≥1, which wouldn't catch erosion). Live buckets:
  real ≥2-head merge (floor 30%, measured 54%), multi-head ≥3-head merge (floor 5%,
  measured 25%), DAG-contains-a-message (floor 30%, measured 69%). The PL-conflict,
  rejected-with-child, and soft-failed-message buckets are deferred to the P4 shadow
  model — they cannot occur in the creator-only profile (alice is omnipotent and
  always joined) — and are marked `TODO(P4)` in the test.

### First milestone (DONE) — validate the structural generator before the shadow model

Implemented as `creator_only_fork_merge_dag_accepts_every_event` in
`crates/neutrino-state/tests/prop.rs`. Order-independence (the flagship property)
also landed on the same generator as `creator_only_fork_merge_order_independent`.

Start with ONE property that drives the full machinery (DAG gen → forks → merges →
`build_local_event`/`apply_pdu` → head-sets → current_state) but whose oracle is
trivially knowable, so any failure is unambiguously a *generator* flaw — caught early.

**M1 = creator-only fork/merge DAG, assert every event is accepted.** Profile: alice
is the sole sender (create + her own join), then forks/merges over state events she's
trivially authorised for — `m.room.topic` / `m.room.name` on divergent branches (and
the occasional alice `m.room.message`), recombined by multi-head merges. Because the
creator is omnipotent (rule-10 implicit-max power), auth *cannot* fail, so:

- Oracle: every `apply_pdu` returns accepted — no `rejected`, no `soft_failed`. Any
  reject ⇒ a structural generator bug (broken `prev_events`/`prev_state_events`
  linkage, topo-order violation, ts collision → duplicate event_id, head mistracking).
- Cheap co-check: folded `UpdateCurrentState` deltas == `room.current_state()` (the
  delta-stream-completeness invariant the persist layer relies on), and the create
  event + alice's membership are always resolved.

Crucially M1 needs only the **structural** generator (head tracking, topo build,
deterministic ts/id, build-once/reuse) — NOT the shadow auth model. It isolates and
hardens the foundation first; the remaining properties (P2/P3/P5) plus the shadow
model + adversarial profile + P4 layer on once M1 is green.

Open generator question (deferred past M1): whether the shadow model's branch-merge
must be faithful enough to keep yield high on deeper forks, or whether approximate-
merge + bounded size is sufficient. Decide empirically from the coverage corpus.

### Next milestone (DONE 2026-06-02) — shadow auth model + multi-user + adversarial

STATUS: COMPLETE. Phase 0 (verdict-aware oracle), Phase 1 (multi-user
shadow-auth adversarial generator + coverage corpus), and Phase 2 (the
properties below) have all landed — see the 2026-06-02 decisions-log entries.
`build_adv_dag` / `apply_adv_dag` / `Shadow` / `AdvOp` / `Verdict` /
`adv_order_independent_invariants` / `adv_rejection_cascade_and_isolation` are
in `crates/neutrino-state/tests/prop.rs`. The only deferred item is idempotency
(P3), skipped per Kegan.

Goal: lift the creator-only generator to **multiple users with real membership /
power-level transitions and deliberate unauthorised events**, then re-run
order-independence + add idempotency + rejection properties on the richer DAGs.
This is where PL/membership *conflict merges* (the mainline-sort path) and
rejection semantics actually get exercised.

**Reusable as-is** (in `crates/neutrino-state/tests/prop.rs`): `Dag`,
`apply_dag(dag, order) -> Outcome`, `random_topo_order` (Kahn over
`prev_events ∪ prev_state_events`), `Outcome`, `heads_after`, the deterministic
topo-index timestamps, and the build-once/replay split. The order-independence
property re-runs unchanged on the new generator. What changes is `DagBuilder` /
`build_dag` (event selection) and the per-event oracle.

**Recommended construction strategy (settled in discussion):** have `build_dag`
drive a **real `RoomCore` in build order during construction** to get true
accept/reject/soft-fail verdicts, and track heads from *reality* (advance only on
the real verdict, mirroring `apply_pdu`'s drop-rejected-from-heads). This removes
head-tracking from the shadow model's burden entirely — the DAG is structurally
valid by construction even with rejects, and a wrong shadow guess only lowers
*yield* (an extra reject), never corrupts structure. The shadow model is then a
lightweight *yield helper* for intent selection only. (Note: this re-introduces a
RoomCore into construction — fine, it still outputs a pure `Dag` for replay.)

**Shadow model** (auth-relevant projection, per head): `members:
Map<UserId,Membership>`, `power_levels`, `join_rule`, creator. Used to *realise*
intents (pick a sender/target that the model says is valid, or for `Unauthorised`
one it says fails). Coverage device, not correctness device — see the M1 notes;
branch-merge may be approximate.

**Generator changes:**
- **Bounded user pool** (alice=creator + bob/carol/dave), `sample::select`.
- **Intent-tagged ops**: `Join | Invite | Leave | Kick | Ban | Unban | Topic |
  Name | PLPromote | PLDemote | Message | Unauthorised(kind)` (replaces today's
  topic-only `Extend`/`Fork`/`Merge`/`Message`; fork/merge become a head-selection
  dimension orthogonal to intent).
- **Explicit conflict-fork primitive**: off one head, two branches mutate the
  *same* key (same membership target or same PL `users` entry), then a merge —
  guarantees the mainline-sort / IAC conflict path is hit (random head-selection
  rarely collides).
- **PLPromote/PLDemote**: both valid (creator raises/lowers a lesser user) and
  invalid (peer demotes a peer at/above own level — rule 10 forbids).

**Properties on the richer DAGs:**
- **Order-independence** (reuse `creator_only_…_order_independent` shape): holds
  regardless of accept/reject; verdicts are a pure function of DAG ancestry.
- **Rejection determinism**: each event's verdict (accept / reject / soft-fail) is
  identical across all application orders.
- **Rejection cascade + isolation**: an event whose `prev_state_events` names a
  rejected event is itself rejected; a rejected event never appears in
  `current_state`.
- **Idempotency**: after applying the DAG, re-apply a random subset in any order →
  `current_state` + head-sets unchanged (the persisted-check guards this).
- The M1 "every event accepted" oracle is **dropped** (we now make rejects on
  purpose); replace with "real verdict == shadow prediction" as a *yield/coverage*
  check, not a hard correctness gate.

**Coverage corpus** (separate non-shrinking test): assert the generator actually
produced a PL-conflict merge, a rejected-with-dependent-child, a soft-failed
message, and a multi-head merge — else the properties pass vacuously.

**Bounds & open question:** keep DAGs small (≤ ~20 events, fork-width ≤ ~3) as the
primary desync control; decide empirically (from the coverage corpus) whether the
shadow branch-merge needs to be faithful or approximate-merge suffices.

## open questions
- how should low bandwidth CBOR/CoAP integrate with HTTP/JSON? As a separate crate/proxy or baked into the Event / Request / Response types? How does this affect working with Ruma?



## stack
- framework: axum + tokio
- serialization: serde + serde_json + some cbor library
- error handling: thiserror
- testing: cargo test + httptest

## architecture
storage is behind a StorageBackend trait — do not couple handlers to a concrete type.
SQLite is the only concrete implementation for now.
all handlers return Result<Json<T>, AppError>. AppError implements IntoResponse.
never use .unwrap() in handler code.

## non-goals (do not implement)
- Rate limiting
- Any authentication (access tokens) on the client-server API
- Tracing

 ## decisions log

Decided to use Claude.

2026-06-03: Outbound federation `/send` — plan + PR1 landed (Kegan signed off). **Plan:** 4 PRs — (1) outbox population, (2) reqwest `FederationClient` + resolver, (3) per-destination sender pool + startup wiring, (4) reqwest `MissingEventsFetcher`. Decisions: **reqwest** added (`--no-default-features --features json`, http-only, no TLS — trusted mesh on raw IPs); **server_name→URL resolution** = `http://{server_name}` (no .well-known/SRV); **wiring lives in `neutrino-http::serve`** not neutrino-main (neutrino-main lacks the state/sqlite deps, same reason as Phase-6d PR3); **txnId** = `startup_ts || in-memory counter` (uuid-free; receiver `record_federation_txn` dedups); **sequenced before** the inbound stage-then-async redesign so that work can reuse the reqwest client + restart-wiring. **PR1:** the "never lose" anchor is atomicity — `persist_resolved_event` writes `outbox` rows **in the same txn** as the event (`destinations: &[&ServerName]` param; shared `insert_outbox_rows` helper). Destinations computed by the actor from **post-apply** `current_state`: join-member servers **+** (for `m.room.member`) the event's target server, minus own — matches Synapse "hosts in room + target" so leave/kick/ban reaches the departing server, which has already left the joined set. Local sends enqueue; received PDUs (`ApplyPdu`) pass `&[]` (not re-originated by us). Own server stored as the `String` config form on `RoomRegistry`/`RoomActor` (only ever compared by value → no fallible parse in infallible setup code). No sender task yet — outbox accumulates until PR3. **Review fixups (5 fresh-context subagents, commit 1db193e):** (1) the `+target server` clause now fires only for **departing** memberships (`leave`/`ban`) — invites/knocks are delivered via the dedicated `/invite` handshake, not `/send` broadcast, and a `join` target is already in the post-apply joined set; this also subsumes the prev-vs-post-state concern since `post-apply-join ∪ departing-target ⊇ prev-state-join` always (a membership change is keyed to the affected user). (3) a soft-failed **non-state** event is not federated (failed auth vs current state → peers would reject); unrejected state events always federate. (4) `member_is_join` → `event_membership` reusing `neutrino_store::Membership::from_wire` (drops the `"join"` magic string). Tests: `setup_with_remote_member` now asserts zara is genuinely joined in current_state (apply_pdu returns `Ok` even for rejects), + new `invite_of_remote_user_is_not_federated_via_send`. Deferred (Kegan-noted, not done): own_server as parsed `OwnedServerName`, relocating `outbound_destinations` to neutrino-state, multi-server/idempotency tests.

2026-06-03: Inbound `/send` review (commit 64b440f, 5 fresh-context subagents). **BUG-1 fixed**: `federation::send::toposort` didn't dedup PDUs, so a transaction repeating the same PDU bytes (with an in-batch child referencing it) double-decremented the child's `indegree` (`usize` underflow → debug panic). Now `parsed` is deduped by event_id (keep first; `apply_pdu` is idempotent regardless). +1 regression test. **BUG-2 (txn recorded processed before PDUs applied → loss on retry) NOT fixed now — deferred into a larger redesign** (Kegan): rework inbound `/send` from synchronous-process to **stage-then-async-process** — durably stage parsed PDUs to a new staging table + 200 immediately, then a background worker drains per-room through the existing actor (gap-fill loop moves off the request path), restart-resumable via neutrino-main enumeration. RoomCore's persisted-check already gives PDU-application idempotency, so `/send`'s job is *durable accept + don't wedge*, not full sync processing. **Keep the `txn_id` dedup** as a cheap whole-txn short-circuit alongside per-event_id staging dedup (Kegan). Needs a StorageBackend staging sub-trait (sign-off + log before building). Sequencing: likely after outbound delivery (shares restart-wiring + reqwest fetcher). Full task breakdown recorded under the Server-Server /send status item. Spec-conformance review found no hard v1.18/MSC4242 violations; remaining review findings (fault-vs-no-progress in `MissingEventsFetcher`, swallowed persist error, state-FE gap-fill boundary, UnknownRoom terminal, gap-fill-success untested) are all inert under `NoFetcher` and fold into the redesign / outbound work.

2026-06-03: Inbound `PUT /_matrix/federation/v1/send/{txnId}` (`crates/neutrino-http/src/federation/send.rs`, 9 e2e tests). Scoped **inbound-only** (Kegan) — outbound delivery is a follow-up. **No ruma `federation-api`**: that feature on our pinned ruma 0.15.1 depends on `ruma-federation-api ^0.14.0`, which is unpublished (crates.io tops out at 0.13.1) — so `cargo add … --features federation-api` fails to resolve. We don't need it: `backfill.rs`/`get_missing_events.rs` already hand-roll serde mirrors with `pdus: Vec<Box<RawValue>>`, so `TransactionBody` does the same (PDUs opaque, EDUs deserialized-for-shape then dropped). The attempted Cargo.toml feature add was reverted. **PDU parse** reuses `event_id::from_wire` (derive-don't-duplicate: it computes the id from the reference hash, redacts on content-hash mismatch, runs format+semantic validators) rather than open-coding compute_event_id + parse_event; an unkeyable parse failure is silently skipped (no id to key the result under — Synapse parity). **Toposort** is intra-batch Kahn over `prev_events ∪ prev_state_events` so a parent applies before a same-txn child. **Per-PDU result** map per spec; a persisted reject is `{}` (success — federation persists rejects), only an unevaluable PDU (unfillable gap / non-retryable DROP / unknown room / fault) carries `error`. **Gap-fill termination** (the question Kegan raised): under MSC4242 completeness is structural — the state DAG must walk to `m.room.create`, which is exactly what flips `apply_pdu` from retryable-Err to Ok, so we don't separately test "reached create". Three terminal conditions: (1) `apply_pdu` Ok, (2) a fetch round returns zero new events (unfillable → error that PDU, others commit), (3) `MAX_GAPFILL_ROUNDS` backstop. Fetch requests `state_dag: true` with an exponentially-growing `limit` (per MSC). **Key coupling decision**: the gap-fill's outbound `/get_missing_events` call needs an HTTP client (reqwest) + server-name resolution — the deferred outbound infra. Rather than pull reqwest in now, the fetch sits behind a `MissingEventsFetcher` trait wired to a `NoFetcher` no-op; the full loop logic is real + testable, and a retryable PDU hits condition 2 until the reqwest impl drops in (zero handler change). Net: inbound `/send` works end-to-end today for in-order/complete-ancestry txns. `RoomRegistry::apply_pdu` / `Command::ApplyPdu` lost their `#[allow(dead_code)]` (now wired). fmt + clippy -p neutrino-http --tests -D warnings (default + multi-user-shim) + cargo test -p neutrino-http clean (147 lib incl. 9 new send tests).

2026-06-03: Consolidated `crates/neutrino-state/tests/prop.rs` (3034 → 2498 lines, 38 → 32 tests) now that the multi-user adversarial generator subsumes the creator-only one. **Retired the entire creator-only fork/merge apparatus** (`DagOp`/`dag_op`/`heads_after`/`DagBuilder`/`build_dag`/`apply_dag`/`Outcome`, both `creator_only_*` properties, `fork_merge_generator_coverage_corpus`). Shared scaffolding (`Dag`, `Verdict`, `classify`, `persist_effect`, `expected_heads`, `random_topo_order`, `build_create`/`build_join`) stays — the adversarial suite already reuses it; a creator-only DAG is just an adversarial DAG where nothing rejects/soft-fails. **Two of the creator-only suite's invariants transferred**: (a) the `UpdateCurrentState` delta-fold == resolved `current_state` cross-check folded into `apply_adv_dag` (holds for any verdict — a rejected event emits no delta); (b) P5 message-isolation + create/creator-membership presence folded into `adv_build_order_reproduces_verdicts_and_heads`. **One invariant did NOT transfer and was dropped: positive-acceptance ("legitimate creator events are accepted").** First attempt asserted "creator event with no rejected `prev_state` ancestor ⇒ Accepted" but it fails on `[Extend Leave, Extend Invite]`: alice self-leaves, then `pick_joined` falls back to the now-unjoined creator for an invite, which *legitimately* rejects (inviter must be joined). Creator implicit-max *power* does not exempt creator events from the membership requirement, and the adversarial generator explores unjoined-creator states the creator-only generator never did — so the all-accepted oracle is inherent to the always-joined creator-only profile and cannot be re-expressed over adversarial DAGs without re-implementing the auth engine. Kegan signed off on the loss (timeline-FE order-independence over the all-accepted subset also goes with it). **Tier-1 merges (no coverage loss)**: 3 `separate_*` structural props → `separate_structural_invariants`; `sort_output_is_permutation` + `sort_parents_come_before_children` → `sort_is_topological_permutation`; `power_of_sender` props 1+3 → `power_of_sender_creator_max_else_pl_lookup` (unified expectation = creator?MAX:PL-lookup, sender picked creator-or-arbitrary via `proptest::option`); `rule_5_3_1_*` reuses `arb_state_with_create` (room id via `room_id_from_create`); `arb_state_set` alias inlined to `arb_state_map`. **Tier-2 subsumption drops (tiny edge loss)**: `separate_single_input_all_unconflicted` + `separate_identical_inputs` (subsumed by `separate_unconflicted_iff_all_sets_agree`); `acd_identical_state_sets_empty` + `acd_order_independent` (subsumed by `acd_equals_union_minus_intersection`, kept `acd_zero_or_one` for the short-circuit). fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state --test prop clean (32 prop tests).

2026-06-03: P3 (idempotency) landed as `adv_replay_is_idempotent` (`crates/neutrino-state/tests/prop.rs`) — the last outstanding fork/merge property. Scoped to ride P1's creator-only DAG, but that generator was retired earlier today, so it rides `build_adv_dag` instead — a strict superset, since the replay subset now also re-applies persisted rejects + soft-fails. Apply the whole DAG in build order, snapshot `current_state` + both head-sets, then re-apply an arbitrary index multiset (`replay in vec(u8, 0..32)`, mod n): every re-apply must return empty effects (`apply_pdu`'s persisted-check at `room_core.rs:330` returns `Ok(vec![])` before any mutation — the path federation transaction-retries rely on) and the post-replay state + head-sets must equal the snapshot. Non-vacuous (replay averages ~16 ids, n≥2 always). All of P1–P5 are now covered by the adversarial suite. fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state --test prop clean (33 prop tests).

2026-06-02: Phase 2 properties on the adversarial generator (`crates/neutrino-state/tests/prop.rs`). Two new `proptest!`s + a `reject_projection` helper. **`adv_order_independent_invariants`**: build the DAG once, apply in build order + 2–4 random topological orders (`random_topo_order`), and assert the three *order-invariant* quantities are identical — `current_state`, the state forward extremities, and the per-event **reject-or-not projection**. These are pure functions of the DAG: state events never soft-fail, and a reject depends only on `prev_state_events` ancestry. **Deliberately NOT asserted order-invariant**: the timeline forward extremities and the `SoftFailed`-vs-`Accepted` distinction — soft-fail is checked against the live resolved `current_state` at apply time (`room_core.rs:433`), so it legitimately varies with which concurrent events have been applied (real Matrix soft-fail semantics). This is why the verdict is *projected* to rejected-or-not before comparison; comparing full verdicts would be wrong, not just flaky. Stress-tested green at 3000 cases. **`adv_rejection_cascade_and_isolation`**: cascade — any event whose `prev_state_events` names a rejected event is itself rejected (`validate_references` → `PrevStateRejected`), checked structurally on the stored verdicts; isolation — a rejected event never appears in the resolved `current_state`, checked against an applied outcome (moved here from the Phase 1 property, which no longer duplicates it). **Idempotency (P3) remains skipped** per Kegan's earlier call. fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state clean (216 lib + 43 prop).

2026-06-02: Shadow-auth multi-user adversarial generator — Phase 0 + Phase 1 (`crates/neutrino-state/tests/prop.rs`; properties = Phase 2, deferred). **Phase 0**: `Dag` now carries a parallel `verdicts: Vec<Verdict>` (`Accepted`/`Rejected`/`SoftFailed`), and `expected_heads` is verdict-aware — only an `Accepted` event is head-eligible or drops the parents it names, mirroring `apply_pdu` exactly (a `Rejected` event mutates no head-set; a `SoftFailed` non-state event is persisted but is neither a timeline head nor drops its `prev_events` parents — synapse#5269). On the creator-only profile every verdict is `Accepted`, so it reduces to the original "FE = unreferenced event" rule (existing 3 creator-only tests unchanged = the gate). **Phase 1**: a second generator (`build_adv_dag`) that drives a *real* `RoomCore` in build order — heads tracked from reality, the real accept/reject/soft-fail verdict recorded per event, output still a pure replayable `Dag`. The shadow model (`Shadow::from_state`, an auth-relevant projection of resolved `current_state`) is **only a yield helper** for intent selection; a wrong guess lowers accepted-event yield but never corrupts the DAG, since verdicts come from `apply_pdu`. User pool alice(creator)+bob/carol/dave; intent-tagged ops (`Join|Invite|Leave|Kick|Ban|Unban|Topic|Name|PlPromote|PlDemote`) crossed with head-selection (`Extend|Fork|Merge|ConflictFork`), plus `Cascade`/`Unauthorised`/`Message`/`StaleMessage`. fork-width ≤4, ≤~20 events/DAG. Coverage primitives: `ConflictFork` (two PL siblings editing one user to different levels) + a later `Merge` ⇒ PL-conflict merge; `Unauthorised` (power-0 non-creator sets a name) ⇒ reliable rejects; `Cascade` (child's `prev_state_events` names the last reject) ⇒ reference-rejection cascade; `StaleMessage` (join victim → kick → victim messages off the *pre-kick* heads) ⇒ a deterministic soft-fail. Two checks: `adv_build_order_reproduces_verdicts_and_heads` (replaying the persisted events in build order reproduces the recorded verdicts + verdict-aware `expected_heads`; rejects never reach `current_state`) and `adv_generator_coverage_corpus` (2000-case deterministic floors: multihead ≥15% [measured 31%], pl-conflict ≥12% [29%], rejected-with-child ≥18% [36%], soft-failed-msg ≥20% [42%] — closes the three `TODO(P4)` buckets). **Recorded for Phase 2**: `reject` is order-independent (pure function of `prev_state_events` ancestry) but `soft-fail` is NOT — `apply_pdu` checks it against the live resolved `current_state` (`room_core.rs:433`), which depends on application order. So Phase 2 order-independence must assert invariance only over {`current_state`, `state_fes`, reject-verdicts}, not the timeline side / soft-fail verdicts. fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state clean (216 lib + 40 prop). Pending fresh-context subagent review before Phase 2.

2026-06-02: P5 (message-event invariants) + coverage corpus test landed on the creator-only fork/merge generator (`crates/neutrino-state/tests/prop.rs`). **P5** folded into `creator_only_fork_merge_dag_accepts_every_event` (rides P2's existing `outcome` + `expected_heads`, no extra build/apply): asserts no message id leaks into `current_state` or the state forward extremities, and the *one-directional* `timeline_fes \ state_fes ⊆ {messages}`. Decision: assert only that direction, not the symmetric difference — the reverse (`state_fes \ timeline_fes`) legitimately contains *state* events: a state head that a later `m.room.message` named in `prev_events` stays a state FE but drops out of the timeline FEs (the message permanently forks the timeline DAG), so a symmetric-difference⊆messages claim would be false. **Coverage corpus** = new `fork_merge_generator_coverage_corpus`, a plain `#[test]` (not `proptest!`) driving `dag_op()` 2000× against `TestRunner::deterministic()`, classifying each DAG, and asserting floor *fractions* (≥1 wouldn't catch erosion, only disappearance): real ≥2-head merge ≥30% (measured 54%), multi-head ≥3-head merge ≥5% (measured 25%), DAG-has-a-message ≥30% (measured 69%). Two enabling generator tweaks: (a) **`DagOp::Merge` now merges *all* live state heads**, not just the first two — so a merge off ≥3 concurrent heads is a genuine multi-head merge (review TEST4/SPEC7: ≥3-way merges were previously impossible); (b) **`Message` weight 1→2** so the P5 timeline/state divergence is exercised more often. The PL-conflict / rejected-with-child / soft-failed-message corpus buckets are `TODO(P4)`: they need the multi-user shadow model (alice is omnipotent + always joined, so nothing rejects or soft-fails here). fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state clean (216 lib + 38 prop).

2026-06-02: Order-independence proptest for RoomCore fork/merge — `creator_only_fork_merge_order_independent` in `crates/neutrino-state/tests/prop.rs`. Refactored the generator to split **construction from application**: `build_dag(ops) -> Dag` builds the event list purely (no RoomCore; mirrors `apply_pdu`'s head bookkeeping via `heads_after`), and `apply_dag(dag, order) -> Outcome` drives a fresh RoomCore applying events in a given index order, returning the order-invariant quantities (`current_state` id-map + both head-sets) plus the per-event accept + delta-accumulation assertions. The property: build the DAG once, apply it in 2–4 random topological orders (Kahn with an entropy-driven ready-set pick) and assert all `Outcome`s match the build-order baseline. **Key correctness point: topo-sort over `prev_events ∪ prev_state_events` (both edge kinds), not just state edges** — `apply_pdu`'s head removal is `heads \ prevs`, so a child applied before a parent would fail to drop that parent and the final head-sets would diverge; ordering every parent first makes the head-sets a pure structural function (events unreferenced by any applied event) and hence order-invariant. Soundness rests on creator-only ⇒ nothing soft-failed (soft-fail is the one thing that *would* make head advancement order-sensitive). Non-vacuity confirmed (forks create concurrent ready heads ⇒ entropy genuinely reorders; verified a forky DAG yields distinct orders). The earlier `apply_state`/`apply_accepted`/`verify` methods on the old `ForkMergeRun` collapsed into `DagBuilder` (build) + `apply_dag` (apply). fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state clean (216 lib + 37 prop; 2500-case order-independence run green).

2026-06-02: First RoomCore fork/merge proptest landed — `creator_only_fork_merge_dag_accepts_every_event` in `crates/neutrino-state/tests/prop.rs`. Structural-generator-only (alice is sole sender + creator ⇒ implicit max power ⇒ every event auth-passes), so the oracle is trivial: `apply_pdu` must accept every generated event (none `rejected`, none `soft_failed`) — any rejection is unambiguously a generator flaw (bad prev_events/prev_state_events linkage, topo-order violation, ts collision → duplicate event_id, head mistracking) rather than an auth verdict. A `DagOp` recipe (`Extend`/`Fork`/`Merge`/`Message`, weighted 3/3/3/1, length 1..14) drives a real `RoomCore`; the generator mirrors `apply_pdu`'s head bookkeeping as `(heads \ prevs) ∪ {E}`. Two decisions worth recording: (a) **insert the `Effect::Persist` event, not the original** — `apply_pdu` stamps the computed `auth_events` (MSC4242 sole authority) and `InMemoryStateProvider::auth_chain` walks them, so inserting the pre-apply event (auth_events empty) would break state-res over merge heads; (b) **timestamps are `base + counter`, monotonic and built once** — origin_server_ts is in the reference hash so distinct ts ⇒ distinct ids, and a global mutable counter would be proptest-non-reproducible. Co-check: folding every emitted `Effect::UpdateCurrentState` delta (start `{}`; `Some`→set, `None`→remove) must reconstruct the resolved `current_state`. (The first cut compared `current_state` to a fresh `state_at_heads(state_forward_extremities)`, but Kegan flagged that as near-tautological: `apply_pdu` already *sets* `current_state = state_at_heads(new_fes)` on every state event — a full recompute, not delta-maintained — so the check was `state_at_heads` warm-cache vs fresh-cache, i.e. only cache-invariance. The delta-fold travels a genuinely different path and tests delta completeness — the invariant the persist layer relies on. The true storage-layer incremental==batch — driving deltas into the sqlite `current_state` table vs a from-scratch resolution — is a separate later milestone.) The shadow auth model + adversarial profile + order-independence/idempotency/rejection properties layer on next. fmt + clippy -p neutrino-state --tests -D warnings + cargo test -p neutrino-state clean (36 prop tests; 2000-case run green).

2026-06-01: createRoom honours `preset` / `visibility` (C1 from the self-review). Previously `join_rules` was hard-coded to `invite`, so a client requesting a public room silently got a private one. Now `join_rule_for(body)` resolves it per spec: an explicit `preset` wins (`public_chat` → `public`; `private_chat` / `trusted_private_chat` / unrecognised → `invite`), else it's derived from `visibility` (`public` → `public`, else `invite`). Also added the `m.room.history_visibility` event (`shared` — the value every standard preset agrees on) emitted in spec order, right after `join_rules`. Still **not** honoured: room aliases, `m.room.guest_access` (guests are out of scope), arbitrary `initial_state`, and `power_level_content_override`; `trusted_private_chat`'s invitee-power bump isn't modelled because createRoom doesn't process the `invite` list. 3 unit tests for the preset→join_rule matrix + 1 e2e (`create_public_room_then_send_succeeds`). fmt + clippy -D warnings + cargo test clean.

2026-06-01: createRoom initial-state batch made spec-compliant + I1 fixed. createRoom previously emitted only create + creator-join (+ optional name), and the name event's `prev_state_events` pointed at the create event rather than the join — so the state DAG had two heads `{join, name}` while `create_room` seeded the FE columns from `initial_events.last()` (= name). When the per-room actor bootstrapped a named room it loaded `state_forward_extremities = [name]`, whose `state_at_heads` walk missed the creator's join → the next send hard-failed auth (sender not in room) → 403 (I1 from the registry.diff review). Fix (Kegan's steer): build the spec-mandated sequence **create → join → m.room.power_levels → m.room.join_rules** (+ name/topic when the `/createRoom` body carries them; history-visibility and aliases deferred, preset/visibility parsing remains a non-goal so `join_rules` defaults to `invite`) by driving a **transient `RoomCore` + `InMemoryStateProvider`**: each event is built on the running heads via the new `RoomCore::build_local_event` (lifted out of the actor's old private `build_event`, which now delegates — one builder, no duplication), gets server-computed `auth_events`, and is **verified through `RoomCore::apply`** before persistence. The chain is linear by construction (each event sits on the single current head), so `create_room`'s existing `last()` FE seeding is now correct and `rooms.rs` needed no change. createRoom still persists as one atomic `create_room` txn (the actor bootstraps lazily afterward — the PR3/PR4 decision stands). Default power levels leave `users` empty (v12 creator is implicitly all-powerful; rule 10.4 forbids naming a creator in `users`). New `CreateRoomError` (Build/Apply → 500, server-authored events). 1 new e2e regression (`create_room_with_name_then_send_succeeds`). fmt + clippy --workspace --all-targets -D warnings + cargo test --workspace all clean.

2026-06-01: Phase 6d PR3 review fixup (I4) — storage no longer names the state machine. The PR3 bridge `SqliteStore::apply_event(room, event) -> (RoomCore, Result<Vec<Effect>, CoreError>)` made `neutrino-store-sqlite` import `RoomCore`/`Effect`/`CoreError` and put the apply call behind a storage method (a layering inversion: storage depending on the state-machine surface). Replaced with a generic provider-scope runner `pub async fn with_state_provider<F, R>(&self, f: F) -> Result<R, StorageError> where F: for<'a> FnOnce(&'a dyn StateProvider) -> R + Send + 'static, R: Send + 'static` — it opens a reader connection, builds the `pub(crate)` `SqliteStateProvider`, and calls `f(&provider)` on the blocking thread. The per-room actor in `neutrino-http` owns the `room.apply(event, provider)` call inside the closure and threads `(RoomCore, verdict)` out as `R`. Net effect: store-sqlite names only the `StateProvider` trait it already implements (the dep edge that has existed since 6b); the apply types live solely in the caller. This is option A from the I4 discussion (chosen over: B = split apply into pre-fetch + pure decision, principled but a large refactor; C = put apply on a `neutrino-store` trait method, which would push the neutrino-state dep down into the abstract trait crate; D = keep `apply_event`, polish the return type only). Pure refactor, no behavioural change; clippy -D warnings + cargo test clean (neutrino-store-sqlite + neutrino-http).

2026-06-01: `RoomCore::apply_pdu` — base integration path for local + federation events (Kegan's framing: `apply_pdu` is the base, local = build-then-delegate, not a wrapper). Renamed `apply` → `apply_pdu` and folded in three federation realities. (1) **auth_events**: apply_pdu computes them from state-before-event and is the sole authority (MSC4242 strips them from the wire); `build_local_event` no longer sets them (for a local event state-before == current_state, so the result is identical — `createRoom`'s transient-RoomCore batch now persists the apply_pdu-stamped events via the new `persisted_event` helper rather than the build output). (2) **Idempotency** by persisted-check (`provider.get_event(id).is_some()`) not head-membership — federation re-sends constantly; the old head-set guard was a single-owner proxy. (3) **Disposition split**: DROP (`Err`: `RoomMismatch`/`Format` — misrouted or malformed, never persisted, a deterministic property of the event not the RoomCore per Kegan's steer), RETRY (`Err`: missing ancestry / lookup fault — `CoreError::is_retryable()`, caller backfills + re-applies), REJECT (`Ok` with `Event.rejected` — an evaluable event that fails auth-vs-state-before or references a rejected/non-state/different-room prev_state; rejection cascades). Also: an accepted **state event that loses state-res** emits `Persist` alone (empty delta) — it still advances both head-sets, but current_state is unchanged (Kegan's catch). Actor: local `Command::Send` surfaces a reject as 403 (`RoomActorError::Rejected`) and does NOT persist (Synapse parity); federation `RoomRegistry::apply_pdu`/`Command::ApplyPdu` persists everything incl. rejects (recorded, never re-requested) and is the future `PUT /send/{txn}` entry point (currently `#[allow(dead_code)]` + unit-tested only). 4 new room_core tests + 2 actor tests + 2 `is_retryable` tests; 6 auth-fail tests flipped from `Err` to `rejected`; the test helper now inserts into the provider *after* apply (pre-insert would trip the new persisted-check). fmt + clippy --workspace --all-targets -D warnings + cargo test --workspace clean.

2026-06-01: v12 state-res made create-implicit at the auth chokepoint (Kegan's option 2). **The bug** (found while testing apply_pdu's conflict path): v12 excludes `m.room.create` from `auth_events` (`auth_events.rs:36`, correct per spec), but the state-res consumers still required it — `iterative_auth_checks` builds a restricted per-event `auth_map` from `event.auth_events` which for v12 events never contains create → `AuthContext::new` `.expect()`ed create and panicked on any genuine conflict; `power_of_sender` silently returned `0` (not MAX) for creators when create was absent. Linear rooms never conflict, so the existing 25+ state-res tests passed (their fixtures predate the exclusion and carry create in auth_events). **The fix**: `AuthContext::new(room_id, state, provider)` is now the single place that resolves create — from `state` when present, else by deriving the create id from `room_id` (`validate::derive_create_event_id`, now `pub(crate)`) and fetching it via the provider. So every auth check gets create for free and no caller (current or future) can forget to inject it. Chosen over option 1 (inject at each consumer — `iterative_auth_checks` + `power_of_sender` separately) precisely because the chokepoint can't be forgotten. Ripple: `check_auth_rules` gained a `provider` param (a create-resolution failure maps to the new `AuthError::CreateUnavailable`; production callers collapse to is_ok/is_err so the verdict is correct; `power_of_sender` propagates `StateResError` directly). ~45 test call sites updated to pass a provider (create is in their state, so it's an unused empty `InMemoryStateProvider`); 3 `reverse_topological_power_sort` case tests + 2 prop tests gained a seeded create for the placeholder room (they modelled a createless room — the unrealistic shape that masked the bug). The conflict test `apply_state_event_that_loses_state_res_emits_persist_only` now passes. fmt + clippy --workspace --all-targets -D + cargo test --workspace clean.

2026-05-29: Phase 6d PR4 — CSAPI writes wired through the actor. `AppState`/`App` holds `Arc<RoomRegistry>`; `put_event` and the new `put_state` / `put_state_empty_key` handlers funnel through one `send_via_actor` helper → `RoomRegistry::send_event`, returning `{event_id}` (now an `Arc<Event>`, so the redundant `Applied` wrapper was dropped). The old `/send` handler was a placeholder writing **empty `prev_events`** (DAG-linkage deferred to Phase 6) — now real. `sender = config.user_id()` (auth stubbed; the single embedded user). `RoomActorError` maps UnknownRoom→404 / Build→400 / Apply→403 M_FORBIDDEN / else→500. **createRoom stays direct** (`store.create_room`, one atomic txn: create + creator-join + optional name; FE columns seeded by PR3): it is *not* routed through the actor, which preserves the create batch's atomicity (the open Q4 question) — the actor bootstraps the room lazily on the first send afterward, reading the already-correct current_state + heads. Two `/state` routes (`{type}` empty-key + `{type}/{stateKey}`) added since neither existed before. Existing message-send e2e tests pass unchanged. fmt + clippy --workspace --all-targets -D warnings + cargo test all clean.

2026-05-29: Phase 6d PR3 — per-room actor + registry, in **neutrino-http** (corrected from neutrino-main, which lacks the neutrino-state/sqlite deps; neutrino-http's AppState is already concrete to `Arc<SqliteStore>`). The actor is concrete to `SqliteStore` (decision, Kegan). Bridge decision: `run_read`/`run_write` and `SqliteStateProvider` are `pub(crate)`/connection-bound, so the actor in neutrino-http can't build a provider itself; rather than make them public (option 2) or move the actor into the storage crate (option 3), we added a thin `pub SqliteStore::apply_event(room, event) -> Result<(RoomCore, Result<Vec<Effect>, CoreError>), StorageError>` (option 1, Kegan's pick) that runs `RoomCore::apply` against a provider on a reader connection — the apply call sits behind a storage method, but it's a read (no write txn) and the actor still owns the RoomCore + orchestrates build→apply→persist→adopt. createRoom flow decided: `create_room` seeds the FE columns to the last-written event (createRoom emits a linear chain of state events ⇒ that event is the sole head of both DAGs; empty `initial_events` ⇒ the create event), which lets the actor bootstrap a freshly created room (an empty head-set would give a new local event no `prev_events`). The actor applies to a clone of its `RoomCore` and adopts only after `persist_resolved_event` commits (apply hard-reject or storage fault ⇒ in-memory state untouched). Registry double-spawn-guarded (re-check under the lock after the I/O bootstrap). Module `#[allow(dead_code)]` until PR4 wires the CSAPI handlers. 3 direct-drive tests. fmt + clippy --workspace --all-targets -D warnings + cargo test all clean.

2026-05-29: Phase 6d PR2 follow-up — current_state made **delta-driven** + `soft_failed` persisted (decided in discussion; supersedes the per-key-upsert description in the prior PR2 entry). **Delta:** `Effect::UpdateCurrentState` now carries a `StateDelta = BTreeMap<(event_type, state_key), Option<OwnedEventId>>` instead of a full `StateMap` — `apply` diffs the old current_state against the recomputed one (`Some(id)` = set/replace, `None` = remove). This models what we actually want ("add/remove these tuple→event-id mappings") and handles the federation state-merge case (several keys changed, including removals) that a single-key upsert couldn't. **Values are event ids, not `Arc<Event>`** (decision, Kegan): every referenced event is already persisted, so `persist_resolved_event`'s applier derives event_type/state_key/membership straight from the `events` row via `INSERT INTO current_state … SELECT … json_extract(json,'$.content.membership') FROM events WHERE event_id=? ON CONFLICT … DO UPDATE` — 0 affected rows ⇒ the id isn't persisted ⇒ `Internal` (the pre-condition, asserted not assumed). Removals are a plain `DELETE`. `write_into_tx`'s implicit single-key upsert is bypassed (new `EventRow::write_into_tx_no_current_state`); `persist_event`'s direct CSAPI path still uses it. **`soft_failed`:** added `events.soft_failed INTEGER NOT NULL DEFAULT 0 CHECK(0,1)` (no `user_version` bump, same call as `rejected` — no live data) + `Event.soft_failed: bool` (co-located server verdict, same pattern as `rejected`). `apply` stamps the verdict onto the event (`Arc::make_mut`, unique on the non-state branch) and the `soft_failed` field is dropped from `Effect::Persist`. Client-timeline filtering on the column is deferred (separate follow-on; this lays the column+field). fmt + clippy --workspace --all-targets -D warnings + cargo test --workspace all clean (9 new tests across neutrino-state / neutrino-store-sqlite).

2026-05-29: Storage-side apply+persist design (Phase 6d), settled in discussion. **Concurrency:** exactly one `RoomCore` per room, owned by a per-room actor (channel inbox) that serialises applies — task-per-HTTP-request would race two applies on the same forward extremities and corrupt the DAG. HTTP handlers orchestrate (receive PDU → send to actor → await outcome → federation/response); only the txn-bound work lives in storage. **Mutable vs immutable:** persisted events + auth chains are immutable, so the provider reads inside `RoomCore::apply` (`get_event`/`auth_chain`/`state_at_heads`) need **no** write txn — they run on a pooled read connection. The mutable state (forward extremities + current_state) lives in the actor's in-memory `RoomCore`; single owner ⇒ no concurrent writer. This **supersedes the forward-looking note in the entry below**: `apply` does *not* run inside the write txn — it's read-then-mutate and the reads are immutable. The only txn-held primitive is the effect persist (write-only, short). Actor applies to a clone and adopts it only after the persist commits (crash/error safe). **Two head-sets:** `RoomCore` now tracks timeline heads (`forward_extremities`, for `prev_events`) *and* state-DAG heads (`state_forward_extremities`, for `prev_state_events`) — they diverge because a non-state event drops a state event from the timeline heads via `prev_events` without touching the state heads. Persisted as two `rooms` columns (`forward_extremities`, `state_dag_forward_extremities`). **current_state** writes the affected key only (an event changes at most one state key); the full-resolved-map path matters only once federation state-merge lands (commented). createRoom's multi-event atomicity vs feeding events singly through the actor is deferred (commented, not atomic yet). Outbox/destinations deferred to Server-Server /send. Built incrementally as PR1/PR2/PR3 (see status). PR1 (this commit): timeline-head tracking in `RoomCore::apply` + the two schema columns; fmt + clippy --workspace --all-targets -D warnings + cargo test (neutrino-state, neutrino-store-sqlite) all clean. **Review correction (I1):** soft-failed events must NOT advance `forward_extremities` — they are persisted but never become timeline heads, so a locally-originated event never draws its `prev_events` from one (and the parent extremities a soft-failed event references are not dropped, avoiding the extremity leak). Matches Synapse `_calculate_new_extremities` behaviour — see matrix-org/synapse#5269, fixed by #5274. The parent extremities clear only when a non-soft-failed successor referencing the soft-failed event arrives.

2026-05-29: Phase 6d PR2 — storage⇄`RoomCore` bridge. Decision A (confirmed in discussion): the load/persist primitives go **on the `StorageBackend` trait** (only `SqliteStore` impls it, so no other impl rippled). `RoomCore::hydrate(room_id, timeline_fes, state_fes, current_state)` is a plain constructor (not `from_parts`) that rebuilds a RoomCore from stored pieces. `RoomStore::forward_extremities(room_id) -> Option<(timeline, state)>` is the load half; `EventStore::persist_resolved_event(event, timeline_fes, state_fes)` the persist half (one write txn reusing `write_into_tx`, then replacing the two FE columns). current_state is written as a **per-key upsert** (an accepted event changes ≤1 key), not a full resolved-map replace — the full replace only matters once federation state-merge can change several keys at once (deferred, commented). `apply` emits Effects; the **actor** (PR3) does the persisting — Effects do not drive the DB write (FE deltas aren't carried in `Effect`; the actor reads the post-apply head-sets off the RoomCore and passes them as raw types, keeping the trait free of neutrino-state). PR2/PR3 reordered from the original sketch: persistence before the actor (the actor depends on it; the bridge is unit-testable standalone).

2026-05-29: `StateProvider::get_event` made fallible — `Option<Arc<Event>>` → `Result<Option<Arc<Event>>, StateResError>`. Self-review of the Phase 6b diff (sqlite.diff) flagged that `SqliteStateProvider::get_event` swallowed all SQL/hydration errors into `None`, which in `validate_references` becomes `UnknownRoom`/`PrevStateNotFound` → a **hard reject** of an otherwise-valid event on a transient DB fault (and asymmetric with `auth_chain`, which surfaced errors as `Internal`). Now: `Ok(Some)` = found, `Ok(None)` = genuine miss (caller maps to its own verdict), `Err(Internal)` = lookup fault (propagates, never a verdict). In-memory + `ProviderWithLocal` impls return `Ok(...)`, never `Err`. `ReferenceError` gained `Lookup(#[from] StateResError)` so a fault during reference validation propagates as a fault rather than a missing-room verdict; ~17 call sites across `state_res.rs` / `room_core.rs` / `validate.rs` (+ prop/sqlite tests) gained a leading `?`. Forward-looking (I5): the deferred atomic apply+persist needs the state-res read and the persist write to share one write transaction — the provider's `&Connection` borrow composes with that (a `Transaction` derefs to `&Connection`; build provider from `&tx`, read, drop before `commit`). Pinned by `provider_usable_inside_write_transaction` + documented in the module header. fmt + clippy --workspace --all-targets -D warnings + cargo test --workspace all clean.

2026-05-12: StorageBackend split into five sub-traits (RoomStore, EventStore, StateStore, DagStore, FederationOutbox) combined via a StorageBackend supertrait. StoredEvent pre-parses event_id, room_id, event_type, state_key, sender, origin_server_ts alongside raw JSON. StoredPdu extends this with prev_events and prev_state_events (MSC4242). Traits live in neutrino-common::storage. Usage pattern is generic bounds (S: StorageBackend), not dyn StorageBackend. insert_events replaced with persist_event(event, destinations) for atomic event+outbox write. create_room takes all initial events atomically. EventStore::subscribe() returns a watch::Receiver<StreamPos> for push-based sync and federation wakeup (subscribe before querying to avoid TOCTOU). StateStore gains joined_rooms(user_id) and joined_members(room_id) with membership=join filtering.

2026-05-13: StorageBackend extended to six sub-traits. EventStore gains get_client_txn/record_client_txn for CSAPI txnId deduplication across restarts. New FederationInbox sub-trait with record_federation_txn(origin, txn_id) for inbound federation txnId deduplication (returns true if already seen). DagStore::missing_events drops min_depth parameter — depth tracking not implemented. Backfill cursor is implicit: derive frontier from events whose prev_events reference IDs absent from the store; no explicit cursor storage needed. Room IDs must be derived from the reference hash of the create event (room version 12), not randomly generated. Outbox startup wiring (enumerate pending_destinations on boot, spawn sender per destination) goes in neutrino-main.

2026-05-14: StateStore gains invited_rooms(user_id). Kept separate from joined_rooms to keep the trait append-only; may be folded into a single method with a membership filter parameter in the future. Sliding sync (MSC4186) uses ruma::api::client::sync::sync_events::v5 types directly (requires `client-api-s` + `unstable-msc4186` features on ruma); filters (is_dm/is_encrypted/is_invite/room_types/not_room_types) are parsed but ignored — every candidate room is returned (embedded single-user server). bump_stamp = max(origin_server_ts) of timeline events in the room (MSC4186 says "not a timestamp" but field is opaque to the client, so this satisfies sortable-activity semantics and means backfilled-old events don't bump rooms). Lazy members, heroes, expanded_timeline, and state stubs are deferred. Connection state lives in an in-memory ConnRegistry; lost on restart → client gets M_UNKNOWN_POS and re-syncs. Pos token is a per-conn sequence counter, not an event stream position.

2026-05-15: Sliding sync wired into the live router. `InMemoryStore` (in `neutrino-http/src/in_memory_store.rs`) initially provided the `StorageBackend` impl for production; superseded on 2026-05-21 by `SqliteStore::open_in_memory()` from `neutrino-store-sqlite`. `InMemoryStore` is retained as the seeding helper for sliding-sync unit tests. Per-conn idempotency cache (`Conn::last_request_pos`, `Conn::last_response`) lets clients safely retry the same `pos`. State-stub emission is gated behind a hardcoded `EMIT_STATE_STUBS: bool` in `build.rs` (default `false`) — clients keep their stale view of removed state until it's re-set. Tower 0.5 added as a dev-dependency of `neutrino-http` only, for `Router::oneshot` in `tests/e2e_sliding_sync.rs`.

2026-05-18: `StateStore` gains `rooms_with_membership(user_id, memberships: &[&str]) -> Vec<(OwnedRoomId, String)>` returning `(room_id, current_membership)` pairs. Lets sliding sync's `candidate_rooms` enumerate every MSC4186-eligible membership (join / invite / knock / leave / ban) in a single round-trip and branch on the actual membership without a second lookup. `joined_rooms` / `invited_rooms` are now thin wrappers that filter to a single membership, but kept on the trait because the SQLite impl can give them dedicated indices. `InMemoryStore` collapsed its separate `joined` and `invited` maps into one `HashMap<UserId, HashMap<RoomId, String>>` that captures all five membership strings. Inclusion rules (MSC4186 §"Rooms included in the server list"): join/invite/knock always; kick (leave with sender ≠ target) always; self-leave or ban only if previously emitted on the same `Conn` (`conn.sent.contains_key`). The "previously emitted" check approximates MSC4186's "previously joined" — exact only within a single conn's lifetime, false-negatives possible after restart.

2026-05-19: Room state machine lives in a new `neutrino-state` crate (deps: ruma, serde, serde_json, thiserror — no async, no tokio). `StateProvider` will be a sync trait. Provider returns **state-before-event** (the new event sits on top of state-before; state-after doesn't expose that hinge). `prev_state_events` (MSC4242) is read directly off the event, not via the provider. State groups deferred. Soft-fail does not gate current-state updates (matches Synapse: `_check_for_soft_fail` only sets the flag; state-res runs over the DAG unaware of it; the flag gates client relay and `_get_prevs_before_rejected` only). Phase order: 0 types → 1 format → 2 auth_events selection → 3 auth rules → 4a separate+auth_chain_diff → 4b reverse-topo+IAC1 → 4c mainline+IAC2+merge → 5 in-memory provider → 6 RoomCore::apply. IAC pass 1 starts from **empty** state per v12 (not unconflicted).

2026-05-19: MSC4242 removes `auth_events` from the wire (events carry `prev_state_events` instead). Servers calculate `auth_events` server-side from state-before via the selection algorithm. State-res algorithm itself is unchanged from synapse `state/v2.py`; only the input source changes (calculated, not on-wire). `Event` struct does not carry `auth_events`. Phase 1 rejects any incoming event that includes `auth_events` on the wire (cross-ref synapse `events/__init__.py` `assert "auth_events" not in event_dict`). `signatures` field is ignored entirely (trusted-network policy). `hashes` field is required and shape-validated even though we don't verify the values (content hashes are independent of signatures/keys). v12 rule 5.8 (membership ∈ {join,leave,invite,ban,knock}) lives in Phase 3's switch/case, not as an upfront assertion in Phase 1, to avoid sync drift between the assertion and the per-arm handlers. Room ID for `m.room.create` events is derived inline from the caller-provided `event_id` via `$ → !` sigil swap; full reference-hash event_id computation is out of Phase 1 scope.

2026-05-19: Phase 1 split into 1a (pure-JSON wire format) and 1b (referential validation). 1b is `validate::validate_references(event, provider)` and owns: v12 rule 2 (room_id corresponds to an accepted create event) and the MSC4242 `prev_state_events` triad (same-room, has-state-key, not-rejected). Rule 2 moves out of Phase 3 — it's existential, not authorisation. Phase 3 keeps rule 4+ (rules that make decisions on content, not just existence). `apply` orchestration calls 1a → 1b → state-res → 3, never implements validation itself. 1b initially looked blocked on Phase 5 (no provider yet), but was unblocked by introducing a minimal `StateProvider` trait (one method, `get_event`) up front; both 1a and 1b are complete.

2026-05-19: `StateProvider` trait introduced in `neutrino-state/src/provider.rs` ahead of Phase 5 to unblock Phase 1b. Today it carries one method: `get_event(id) -> Option<EventInfo>` where `EventInfo { event: Arc<Event>, rejected: bool }`. The trait grows as later phases land (state-after lookups, auth-chain difference, power-level auth ancestor). Rejection is bundled into `EventInfo` rather than a separate `is_rejected` call to keep lookups single-shot. `ReferenceError` lives in `lib.rs` as a sibling of `FormatError`/`AuthError` (not folded into FormatError — different failure mode: provider lookup vs. wire shape). Create events bypass `validate_references` entirely (no room_id, no prev_state_events to check).

2026-05-20: Proptest strategies use `prop_filter_map` (returns `Option<Event>`) instead of `prop_map(...).expect(...)`, so a future parse-rule tightening that rejects a corner of the strategy space discards inputs rather than panicking the suite. Member-event strategy avoids `prop_filter` by choosing memberships via `prop_flat_map` + `proptest::sample::select`: when `sender == target` the wire-impossible memberships (`invite`, `ban`) are dropped from the pool by construction. The "no fabrication" property on `calculate_auth_events` was upgraded to a set-equality property: result == `auth_event_keys(event)` ∩ `state` — a no-op `vec![]` implementation no longer satisfies it. Added a universal create-exclusion property that injects `(m.room.create, "")` into the state map and asserts the value never appears in the result. `parse_event_rejects_any_missing_required_field` was a proptest enumerating a 9-element array — demoted to a plain `#[test]` loop.

2026-05-20: Phase 3 v12 auth rules in `auth_rules.rs`. Public `check_auth_rules(event, state: &StateMap<Arc<Event>>) -> Result<(), AuthError>` — pure function, no provider. Dispatcher visits rules in spec order 1→11 with one slot per rule (cross-phase rules left as comments rather than dropped, for grep-from-spec verifiability). Internal sub-functions and `AuthError` variants named after rule numbers (`Rule5_3_2_JoinSenderMismatch`, `check_rule_5_3_join`, etc.; `#[allow(non_camel_case_types)]` at the enum level). `AuthContext` pre-computes creators (sender + additional_creators with i64::MAX power), `PowerLevels` (typed struct with field-level defaults: users_default=0, events_default=0, state_default=50, ban=50, kick=50, redact=50, invite=0), join_rule (default "invite"), required_power_for(event). Rule 5 dispatches on membership: 5.3 (join), 5.4 (invite), 5.5 (leave/kick), 5.6 (ban), 5.7 (knock), 5.8 (unknown). Rule 5.2 / 5.4.1 signature verification skipped per trusted-network — third-party invites accepted on field-presence of `public_key(s)`. Rule 10 inherits 10.1–10.3 from Phase 1a; 10.4–10.11 walk scalar/events/notifications/users diffs against sender power. 24 tests covering happy-path + reject path for each rule.

2026-05-20: Phase 3 review fixes. Removed `AuthError::CreateMissing` — `validate::validate_references` guarantees the create event is in state-before-event, so `AuthContext::new` now `.expect`s it (the proptest, which previously exercised the absent path, now always seeds a create event). Added `AuthError::InvalidMemberStateKey { state_key }` used by 5.4 / 5.5 / 5.6 in place of the misleading `Rule5_4_1_ThirdPartyInviteInvalid` / `Rule5_5_5_KickNotAllowed` / `Rule5_6_3_BanNotAllowed` errors the parse-failure path was conflating with rule violations. `Rule5_5_5_KickNotAllowed` and `Rule5_6_3_BanNotAllowed` now carry `{sender, target, kick_level/ban_level}` so debugging shows which conjunct failed. `AuthContext` precomputes `federate: bool` and `create_domain: String` so rule 4 no longer reparses create content per call. `parse_content`, `PowerLevels::parse`, and additional_creators handling moved from silent-fallback to `.expect` citing the Phase 1a invariant — a future Phase 1a regression now panics at the parse site instead of silently substituting defaults. Rule 10.7 / 10.8 now compare *effective* values (`get(...).unwrap_or(events_default)` for events; default 50 for notifications) so setting an entry explicitly equal to the prevailing default is correctly recognised as a no-op rather than a "change". Proptest 5.3.1 coverage fix: `arb_state_with_create` couples the create event_id with the create `Event` inside a single `prop_filter_map` and inserts the explicit create *after* extras so a sampled `m.room.create` in extras can't overwrite it; new directed property `rule_5_3_1_self_join_after_create_always_allowed` builds a self-join whose `prev_events` / `prev_state_events` are both `[create_id]` and asserts it is always allowed. New case tests: 5.3.5 (restricted-join authoriser — happy path + 3 reject paths), 5.4.1 (third-party invite path — happy path + 3 reject paths), rule 7 (`m.room.third_party_invite` event — creator-allow + low-power-reject), 10.6.1 / 10.7 / 10.9 with non-creator PL sender, 10.7/10.8 explicit-at-default no-op, three creator-vs-creator cases (10.4 additional creator in users, creator-cannot-kick-creator, creator-can-invite-creator), `Rule5_5_5_KickNotAllowed` payload pinning, and `InvalidMemberStateKey` on invite. 5.5.3-unban-falls-through-to-5.5.4-kick-gate left as-is per v12 spec text (5.5.3 is reject-only, 5.5.4 is allow-only, 5.5.5 final reject — unban requires both `>= ban` and the kick conjuncts). 112 lib unit + 6 prop tests = 118 in `neutrino-state`; cargo fmt + clippy --all-targets -D warnings + cargo test all clean for `neutrino-state`.

2026-05-19: Phase 2 auth-events selection lands as two functions: `auth_event_keys(event) -> Vec<(String, String)>` is pure-with-respect-to-state and returns the dedup'd `(type, state_key)` pairs the spec asks for; `calculate_auth_events(event, state) -> Vec<OwnedEventId>` resolves those keys against a `StateMap`. Mirrors Synapse's `auth_types_for_event` + `get_events` split, makes the keys testable without state-map setup. v12 excludes `m.room.create` from auth_events — implemented by simply not requesting it. Phase 2 work also surfaced a Phase 1a bug: rule 9 (state_key starting `@` must match sender) was applied to `m.room.member` events, but rule 5 owns that type end-to-end (state_key is the *target* user, not the sender — invites/kicks are normal). Fixed by excluding `m.room.member` from rule 9 in `parse_event`, matching Synapse's `event_auth.py`. New regression test `rule_9_does_not_apply_to_m_room_member`.

2026-05-20: `EventStore` trait split into two persistence paths to resolve the unconditional-`current_state`-UPSERT ambiguity surfaced by the SQLite store review. `persist_event(event, destinations)` keeps today's semantics: forward extension, upserts `current_state` for state events, writes outbox rows. New `persist_historical_event(event)` writes the event + `event_edges` only — no `current_state` upsert, no outbox rows — for `/backfill` and `/get_missing_events` paths that fill in history older than the room's resolved head. Shared write logic lives in `EventRow::write_into_tx_inner(tx, update_current_state: bool)`; the two public wrappers (`write_into_tx` and `write_into_tx_historical`) differ only by that flag. Validation (column ↔ JSON cross-checks, member-event well-formedness) fires on both paths — historical events must still be well-formed for read-side correctness. The watch advances on both paths (subscribers wake to discover new history); only the resolved-state / federation-outbox sides differ. Pinned by six new tests in `events.rs` including the cardinal regression test `persist_historical_event_does_not_regress_current_state`.

2026-05-21: Sliding sync phase 4-6 review fixes. (a) Invited rooms lift `m.room.name` / `m.room.avatar` from `unsigned.invite_room_state` to top-level `room.name` / `room.avatar` (restored from a prior fix that the phase-4 rewrite dropped); member counts stay `None` for invites per Synapse parity. (b) Initial sync anchors `last_event_stream_pos` at the store's current head (via `subscribe().borrow().0`) instead of advancing only by the EVENTS_PER_SYNC_LIMIT-bounded drain — eliminates duplicate-event surfacing on the second sync when the store has > 1000 events. (c) `is_invited` JSON parse failure degrades to `false` instead of erroring the whole sync; defense in depth since `Box<RawValue>` guarantees valid JSON at construction. Mock fix: `MockStore::add_event` switched from `watch_tx.send` (no-ops without receivers) to `send_if_modified` to match the production `notify_watch` pattern.

2026-05-21: Sliding sync `bump_stamp` computation split by membership. `bump_stamp_for_joined` peeks `room_messages` (Backward, 1) with fallback to the create event ts; `bump_stamp_for_invited` looks up the invitee's own `m.room.member` event ts. The previous unified `compute_bump_stamp` fell back to `current_state_event("m.room.create", "")` when `room_messages` was empty, but that path is unreachable for invited rooms (we don't have the create event for rooms we haven't joined) — invites silently got `bump_stamp = 0`. The invitee's member event is the only ts that's guaranteed-present for an invited room (it's how we knew about the invite in the first place). Also: `effective_range` now drops inverted ranges (`a > b`) in addition to out-of-bounds starts; the slicing iterator would silently zero them out otherwise. Phase 6 may upgrade this to a 400.

2026-05-21: Phase 4a review fixes. `StateProvider::auth_event_ids` now returns `Cow<'_, [OwnedEventId]>` (was `Vec<OwnedEventId>`) — in-memory impls borrow their stored vec via `Cow::Borrowed`, DB-backed impls can still return `Cow::Owned` from a row scan. The DFS loops in `conflicted_subgraph` and `auth_chain_difference` now iterate by reference and clone only when pushing onto the stack, skipping the clone for parents already visited (the common case for shared ancestors). Doc on the trait method now flags that an empty result conflates "unknown id" with "known `m.room.create`"; callers needing the distinction must `get_event(id)` first. Added directed test `auth_chain_difference_empty_state_set_returns_other_chain` ([&empty, &non_empty] case). Consolidated proptests `acd_subset_of_union` + `acd_disjoint_from_intersection` into a single equality `acd_equals_union_minus_intersection` — the two-direction split did not pin set equality.

2026-05-20: Schema-bundle loader rewritten to match the 2026-05-14 design-doc shape it had drifted from. `schema.sql` previously held `PRAGMA journal_mode = WAL` at the top *and* `PRAGMA user_version = 1` near the top, and `schema::ensure_schema` ran the whole file via `execute_batch` with no surrounding transaction. A mid-bundle CREATE failure would leave `user_version = 1` paired with a partial schema, and the next open would short-circuit on the `1 => Ok(())` arm of the version gate without ever re-running the bundle. Fix: split the two ambient PRAGMAs out of `schema.sql`. `schema::ensure_schema` now runs `PRAGMA journal_mode = WAL` against the bare connection first (SQLite forbids journal-mode changes inside a transaction), then opens a `conn.transaction()` and runs `execute_batch(SCHEMA_SQL)` followed by `execute_batch("PRAGMA user_version = 1")`, then commits. Mid-bundle failure rolls the DDL *and* the version stamp back atomically; the next open sees `user_version = 0` and re-runs the (non-`IF NOT EXISTS`) bundle, which either succeeds or fails loudly on a colliding pre-existing table — the failure mode the design doc (§2 "Open path") called out. Two new in-crate tests in `schema.rs`: `ensure_schema_refuses_unknown_user_version` exercises the `other => Err(Internal(_))` arm of the version gate (previously uncovered), and `ensure_schema_rolls_back_on_mid_bundle_failure` pre-seeds a colliding `rooms` table on a fresh file, opens the store, and asserts that the bundle's transaction rolls back leaving `user_version = 0` and the pre-existing table intact.

2026-05-21: `main` merged into `kegan/sss-4`. Live router and the e2e sliding-sync HTTP tests now run against `SqliteStore::open_in_memory()` (from `neutrino-store-sqlite`); `router()` became `async` to accommodate the async open. `InMemoryStore` (in `neutrino-http/src/in_memory_store.rs`) is retained but downgraded to a sliding-sync test helper — the only consumers are `sliding_sync::tests`. Phase 4-6 review fixes from `main` (`lift_invite_metadata`, `bump_stamp_for_invited`, initial-sync high-water anchored at store head via `subscribe().borrow().0`, `send_if_modified` watch pattern) merged in; `candidate_rooms` keeps the `rooms_with_membership` enumeration and now dispatches `bump_stamp` by membership (`invite` → `_for_invited`, everything else → `_for_joined`). The `initial_sync_anchors_high_water_at_store_head` regression test ported across to use `InMemoryStore`. `crates/neutrino-sqlite` (the pre-trait stub) is no longer a dependency of `neutrino-http` — `neutrino-store-sqlite` (the actual trait impl) takes its place.

2026-05-22: Phase 5 review fixes. (a) CSAPI write handlers (`create_room`, `put_event` in `neutrino-http::lib`) now propagate `StorageError` as `500 M_UNKNOWN` instead of `let _ = …await` — silent error drops were returning 200 OK with a fresh room_id/event_id for failed writes. (b) `conn.pos` advancement moved out of `build_response` and into `super::handle`, so the long-poll loop's multiple `build_response` calls per request consume exactly one pos value (and a mid-loop storage error leaves `conn.pos` untouched, matching the client's last-good token). `build_response` now uses `conn.pos.saturating_add(1)` for the response's pos_token without mutating `conn.pos`; `handle` commits the bump once, after the loop returns. (c) `is_kick` reads `StoredEvent.sender` directly instead of re-parsing the event JSON — obsoletes the `SyncError::Storage(Internal)` path for malformed member events (no parse, no failure mode). (d) `candidate_rooms` drops the `dedup_by(room_id)` defensive pass; `StateStore::rooms_with_membership` is contracted to return at most one row per room. (e) `InMemoryStore` deleted entirely — every sliding-sync unit test now opens its own file-backed `SqliteStore` on a `tempfile::NamedTempFile`. The `deleted_state_not_surfaced_with_stubs_disabled` integration test is `#[ignore]`d (the trait surface has no way to remove a `current_state` row; deletion detection is unit-tested at `build::unit_tests::diff_required_state_detects_deletion`). `tempfile` added as a dev-dependency of `neutrino-http`. `MSC4186-gaps.md` no longer claims InMemoryStore is the live backend.

2026-05-22: Wire `room_version` is `org.matrix.msc4242.12` (MSC4242 unstable identifier), not the bare `"12"`. Factored into `pub const ROOM_VERSION_ID: &str` in `neutrino-common`; `neutrino-state`, `neutrino-store-sqlite`, `neutrino-http` all reference it. `neutrino-store-sqlite::create_room` now compares the raw JSON `content.room_version` against `ROOM_VERSION_ID` *before* parsing through `RoomVersionId::from_str` — the previous `if room_version != RoomVersionId::V12` guard was wrong because ruma silently maps unknown strings to `RoomVersionId::Custom(...)` rather than erroring, so a future spec-correct bump to the MSC4242 string would have been accepted by `V12 != Custom("...msc4242.12")`. `get_room_version` keeps `Result<Option<RoomVersionId>, _>` — the value round-trips as `RoomVersionId::Custom("org.matrix.msc4242.12")`. `create_room_rejects_non_v12_room_version` now sweeps both `"11"` and `"12"` so the bare-`"12"` regression path stays covered.

2026-05-22: Sliding-sync `handle` now cancels in-flight long-polls when a newer request arrives on the same `(user, conn_id)`. Per-conn cancellation signal lives in a new `ConnEntry { conn: Arc<Mutex<Conn>>, cancel_gen: Arc<watch::Sender<u64>> }` (replaces the bare `Arc<Mutex<Conn>>` in the registry). Mechanism: on a delta request, `entry.cancel()` bumps the gen *before* `entry.conn.lock().await` — the in-flight holder is `select!`-waiting on `cancel_gen.subscribe()` and unwinds. On initial sync (`req.pos == None`), `registry.create` cancels the prior entry on replacement so the orphan wakes promptly. `watch` chosen over `Notify` because `watch` values are sticky (no lost-wakeup window if the holder hasn't yet subscribed at the bump instant). Cancellation semantics for the cancelled request: treat identically to timeout — return last `build_response` output, advance `conn.pos`, write idempotency cache. Byte-identical newer retries hit the freshly-written cache; body-differing retries at the same pos see the advanced `conn.pos` and return `M_UNKNOWN_POS` (one re-init round-trip — accepted, documented in MSC4186-gaps.md). `resolve_conn` helper + `Resolution` enum collapsed into the inlined cache-check inside `handle` — cleaner now that the cache check has to run under the same lock acquisition as the long-poll. Three new tests pin: cancellation latency (cancelled request returns in <2 s instead of its 10 s timeout), initial-sync-replacement also cancels, and the cancelled→cache-write→fresh-process chain.

2026-05-27 (PR 2 / B6 — CSAPI write handlers on EventBuilder; PR 2 complete): `lib.rs::create_room` builds (create, join, optional name) via `EventBuilder`, threading prev_events / prev_state_events. The create event's `room_id` is the sigil-swapped event_id; trailing events reuse it. `lib.rs::put_event` builds a single event with empty prev_events (DAG-head wiring is Phase 6 scope — body match the pre-B6 behaviour of this handler). Both handlers map builder errors to HTTP 500 `M_UNKNOWN` / 400 `M_BAD_JSON`. Deleted `mint_id` and the transitional `stored_event_from_json` helper. `neutrino-state` added as a path dep on `neutrino-http`. 6 e2e tests previously `#[ignore]`d for B6 are un-ignored and pass: `create_room_then_initial_sync_returns_the_room`, `put_event_then_sync_delivers_it_in_timeline`, `idempotent_retry_returns_same_response_bytes`, `long_poll_returns_within_timeout_when_no_events`, `long_poll_wakes_on_concurrent_put_event`, `initial_sync_with_named_room_returns_the_name`. 414 workspace tests passing, 0 failed, 0 ignored. `event-id-design.md` B6 row marked done; PR 2 status header switched to "PR 2 complete".

2026-05-26 (PR 2 / B4 + B5 — debug_assert + test-fixture migration): Three layered changes that together let test fixtures round-trip through the same hash pipeline production code uses.

(1) **B4 — `debug_assert!` in `EventStore::persist_event` / `persist_historical_event`.** A new private `debug_assert_event_id_matches_raw` helper in `crates/neutrino-store-sqlite/src/store/events.rs` calls `neutrino_common::event_id::compute_event_id(&event.raw)` and panics in debug builds if the result differs from `event.event_id`. No-op in release. The check is gated by `#[cfg(debug_assertions)]` so production has zero overhead. A wrong id is a caller bug — either a hand-rolled event_id that doesn't match the reference hash, or a builder regression. New public `pub fn compute_event_id(raw) -> Result<OwnedEventId, ComputeIdError>` in `neutrino-common::event_id` exposes the recompute path for both the assert and for test helpers that don't want to depend on `neutrino-state`'s `EventBuilder`.

(2) **Prerequisite — drop `depth` from `parse_event`.** v12 spec lists `depth` as a required PDU field, but our design says we don't use it (origin_server_ts subsumes everything depth was good for under Synapse's backfill model). Removed the `MissingField("depth")` check, the `DepthOutOfRange` variant + `DEPTH_LIMIT` const, and 4 depth-only test cases in favour of one `ignores_depth_field_when_present` (presence with any int silently tolerated for non-MSC4242-sender interop; absence parses cleanly).

(3) **B5 — migrate `neutrino-state` test fixtures to `EventBuilder`.** auth_events.rs / auth_rules.rs / state_res.rs / tests/prop.rs all moved from `parse_event(raw(json!({...})), eid("$..."), vec![])` onto `EventBuilder::new(sender, type).room_id(...).content(...).build()`. validate.rs's tests stay on `parse_event` directly — those tests exercise parse_event's rejection paths with intentionally-malformed inputs that the builder can't construct. Each migrated module gained a local `next_ts()` (`AtomicU64`-backed monotonic counter) so successive otherwise-identical fixtures hash to distinct event_ids — v11 redaction strips `m.room.message` body and `m.room.name` name from content, so two messages with the same room/sender/ts but different bodies collide on the reference hash. state_res.rs introduced a labelled-`HashMap<&str, OwnedEventId>` indirection so tests can refer to computed event_ids by string label. tests/prop.rs's strategies build events in topological order (chains tail-first, diamonds bottom-up, the `arb_provider_with_ids` strategy generates events in topo order so every parent reference resolves to a real builder-produced event_id).

(4) **Side-effects — test fixtures in `neutrino-store-sqlite` and `neutrino-http`.** B4's debug_assert tripped ~63 sqlite unit tests + 20 sqlite integration tests + 53 sliding-sync tests. All migrated to the same pattern: `make_event` / `make_event_from_json` / `build_stored_event` / `message_with_ts` / etc. drop the caller-supplied event_id parameter, compute it via `compute_event_id(&raw)`, and callers capture the computed id from the returned `Event` if they need it for assertions or `prev_events` linkage. Test helpers in `crates/neutrino-store-sqlite/src/tests.rs`, `crates/neutrino-store-sqlite/tests/common/mod.rs`, and `crates/neutrino-http/src/sliding_sync/tests.rs` all updated.

Three knock-on adjustments worth recording:
- The `CREATE_EVENT_ID` constant in `crates/neutrino-store-sqlite/src/tests.rs` is deleted — the create event's id is now derived from its raw bytes via the same pipeline production code uses, so a fixed literal is meaningless. Tests that needed the create event_id capture it from `store_with_room_and_create` (new helper returning `(SqliteStore, Event)`).
- One sqlite D6 cycle test re-shaped: the old test built `$a:e` with `prev_events=[$a:e]` (a literal self-loop, which can't be expressed under hash-derived ids — fixed-point impossible). Replaced with an `a → b → a` cycle inserted directly into `event_edges` via raw SQL. Same test intent (walker terminates on cycles), faithful to the "storage-corruption defence" framing.
- Two CSAPI e2e tests in `crates/neutrino-http/tests/e2e_sliding_sync.rs` (`put_event_then_sync_delivers_it_in_timeline`, `long_poll_wakes_on_concurrent_put_event`) are now `#[ignore]`d with a comment: they hit `lib.rs::put_event` which still uses `mint_id` for the event_id rather than going through `EventBuilder`. That migration is PR 2 / B6 — explicitly out of scope for this batch. The ignore lifts once B6 lands.

Final state: 404 workspace tests passing, 0 failing, 2 ignored (the B6-blocked e2e tests). fmt + clippy `--all-targets -D warnings` clean across the workspace.

2026-05-26 (PR 2 / B3 — v12 reference-hash test vector): One authored vector in `neutrino-common::event_id::tests::reference_hash_v12_authored_vector`. The spec appendix has no published v12 reference-hash example (only the two v1-shaped content-hash vectors already pinned by B1), so we author one and verify it independently: (1) construct an `m.room.message` input with every field a v12 PDU can carry (type, sender, room_id, content `{msgtype, body}`, prev_events, prev_state_events, origin_server_ts, hashes, unsigned, signatures), (2) hand-trace the expected post-redaction canonical bytes — for m.room.message the V11 content keep-list is empty so `content` collapses to `{}`, `unsigned` is not in the top-level keep-list so it's stripped by redaction, `signatures` IS in the keep-list but we strip it after redaction (spec step), `prev_state_events` is preserved by the MSC4242 wrapper, final keys sorted alphabetically. (3) Independent cross-check: `assert_eq!(reference_hash(input), sha256(EXPECTED_POST_REDACTION))` — RHS runs SHA-256 (FIPS-verified by `sha256_known_vector`) against the human-readable expected bytes, so a regression in `redact_for_hash` or in our MSC4242 carve-out trips this assertion. (4) Pinned event_id `$mY2a13t3rnoKFepL_yWIHDCPjw7WoP1Rem5QJyvom9w` guards against regressions in `b64_url_unpadded` or the `$<43 chars>` format. The pinned literal also gives the event_id format a verbatim canonical example for the project's own documentation. Skipped `ruma-signatures` as a dev-dep cross-check (design doc mentioned it as optional) — the sha256-of-hand-traced-bytes approach gives equivalent independent verification without the extra dep. Total test count moved 411 → 406 because Skye's PR #33 (refactor: remove client_txns) landed via merge and stripped 6 sqlite tests; net B3 addition is +1.

2026-05-26 (PR 2 / B2 — EventBuilder + from_wire): New module `neutrino-state::event_id`. `EventBuilder` is consuming-self-chain (Debug/Clone) with required `sender` + `event_type` at `new`, optional setters for `state_key`, `content`, `room_id`, `prev_events`, `prev_state_events`, `auth_events`, `origin_server_ts`, `unsigned`. `.build()` runs the seven-step v12 / MSC4242 pipeline: (1) skeleton checks — non-create needs room_id (returns `MissingField("room_id")`), content / unsigned must be JSON objects (`InvalidFieldType`); (2) assemble unhashed `serde_json::Map` then `try_from_json_map` → `CanonicalJsonObject` (surfaces float-in-content / out-of-range int / duplicate-key via new `FormatError::NonCanonical(CanonicalJsonError)`); (3) `content_hash` → insert `hashes.sha256: <b64-unpadded>`; (4) `reference_hash` (`.expect`-ed: builder always supplies string `type`, object `content`, object `hashes`); (5) `event_id_from_hash`; (6) for create, `parse_event` derives `room_id` from `event_id` (sigil swap) — *not* injected back into raw, so wire bytes of a create event lack `room_id` per v12 spec; (7) defence-in-depth `parse_event(raw, event_id, auth_events)`. `now_ms()` default for `origin_server_ts` uses `SystemTime::now()`. `from_wire(raw, auth_events) -> Result<Event, FormatError>` is the inbound counterpart: parses raw → CanonicalJsonObject, computes reference_hash, derives event_id, calls parse_event. `ref_hash_error_to_format_error` maps ruma's `RedactionError::{MissingField, InvalidType}` (the latter `#[non_exhaustive]` with wildcard fallback) onto `FormatError::{MissingField, InvalidFieldType}` so the error vocabulary stays consistent with parse_event. Located as a free function in `neutrino-state::event_id` (not on `Event` itself): the inherent-impl path was blocked by the orphan rule (Event lives in `neutrino-common`, parse_event in `neutrino-state`). `b64_unpadded` in `neutrino-common::event_id` promoted from `pub(crate)` to `pub` so the builder can format the content-hash b64 for `hashes.sha256` insertion; `b64_url_unpadded` stays `pub(crate)` (callers should go through `event_id_from_hash`). Prerequisite: dropped `depth` requirement from `parse_event` — `MissingField("depth")` / `DepthOutOfRange` / `DEPTH_LIMIT` all removed; 4 depth-only tests collapsed into a single `ignores_depth_field_when_present` (presence with any int value is silently accepted for non-MSC4242-sender interop; absence parses cleanly). Existing test fixtures keep their `"depth": N` literals untouched — parse_event no longer reads the field, so they ride through unchanged until B5 migrates them onto `EventBuilder`. 13 new tests in `event_id::tests`: builder happy path (create event derives room_id from event_id; create's wire bytes lack room_id; content/unsigned/hashes shape; 32-byte sha256 → 43-char unpadded b64), builder determinism (same inputs → same event_id; different content → different event_id), builder-side `auth_events` are struct-only (not in raw), three skeleton error paths (missing room_id, non-object content, non-object unsigned), `from_wire` round-trips builder output (event_id / room_id / sender / event_type / origin_server_ts all match), `from_wire` round-trips create events with derived room_id, `from_wire` attaches caller-supplied auth_events, `from_wire` rejects non-object root and missing-type input. PR 2 next step: B3 — spec content-hash vectors (two from appendix, already landed in B1 case tests) + author one v12 reference-hash vector.

2026-05-26 (PR 2 / B1 — proptests on event_id): Three properties in `crates/neutrino-common/tests/prop.rs`, generalising B1's case tests. (A) `content_hash_is_invariant_under_arbitrary_unsigned_signatures_hashes` sweeps any JSON value (recursive `arb_json_value` strategy: null / bool / i32 / short string / arrays / objects, depth ≤ 3) as the three strippable fields and asserts the hash is unchanged. (B) `reference_hash_is_invariant_under_arbitrary_unsigned_signatures` is the same property but `hashes` is excluded — `hashes` is in the v11 redaction keep-list, so its value DOES feed the reference hash. (C) `reference_hash_distinguishes_distinct_prev_state_events_lists` generates two arbitrary `prev_state_events` arrays (0-4 entries each) under `prop_assume!(list_a != list_b)` and asserts the reference hashes differ — pins the MSC4242 carve-out across pairs, not just the one in the case test. SHA-256 collision over distinct preimages at 256 bits is negligible; no caveat needed. New dev-dep on `neutrino-common`: `proptest.workspace = true`. Proptest entropy avoids floats and out-of-range integers so `CanonicalJsonValue::try_from` never refuses input. Ruled out: key-shuffling determinism (tests ruma's BTreeMap sort, not our code); event_id/room_id suffix structural equality (already a case test); content-hash sensitivity to non-stripped fields (would need to enumerate the keep-list).

2026-05-26 (PR 2 / B1 — public hash + id functions): `neutrino-common::event_id` gains four `pub` functions on top of B0. `content_hash(&CanonicalJsonObject) -> [u8; 32]` per spec §calculating-the-content-hash-for-an-event: clone, drop `unsigned`/`signatures`/`hashes`, canonical, sha256. `reference_hash(&CanonicalJsonObject) -> Result<[u8; 32], RedactionError>` per spec §calculating-the-reference-hash-for-an-event: clone, drop `unsigned`/`signatures`, run `redact_for_hash` (V12 redaction == V11, with MSC4242 `prev_state_events` carve-out), canonical, sha256. `event_id_from_hash(&[u8; 32]) -> OwnedEventId` = `"$" + url-safe-unpadded-base64(hash)` (43 chars after the sigil, so 44 total). `room_id_from_create(&EventId) -> OwnedRoomId` does the v12 / RoomIdFormatVersion::V2 sigil swap `$` → `!` and otherwise keeps the suffix byte-identical. The two `OwnedEventId::try_from` / `OwnedRoomId::try_from` paths are `.expect`-ed: 43 url-safe-b64 chars after `$` (or `!`) is always syntactically valid. 9 new tests including both spec-appendix content-hash vectors (`5jM4wQpv6lnBo7CLIghJuHdW+s2CMBJPUOGOC89ncos` for the minimally-sized event, `onLKD1bGljeBWQhWZ1kaP9SorVmRQNdN5aM2JYU2n/g` for the redactable-content event — both v1-shaped but valid as byte-level content-hash tests), invariance under unsigned/signatures/hashes, prev_state_events differential coverage (the MSC4242 invariant), event_id format pinning on `[0xff; 32]` (the trailing `8` proves the unaligned 4-bit-payload tail), and the `[0; 32]` zero vector. `b64_unpadded` is the one B0 helper left under `#[allow(dead_code)]` until B2's builder consumes it for the `hashes.sha256` insertion. PR 2 next step: B2 — `EventBuilder` + `Event::from_wire`.

2026-05-26 (PR 2 / B0 — hashing primitives): New private module `neutrino-common::event_id` carries the four B0 helpers consumed by the upcoming `EventBuilder` (in `neutrino-state::event_id`) and `Event::from_wire` (here in `neutrino-common::event`). `canonical(&CanonicalJsonObject) -> Vec<u8>` pinpoints the canonical-JSON serialisation call site (`serde_json::to_vec` against a `CanonicalJsonObject` is already canonical since the value tree is sorted-keyed and integer-range-checked; the `.expect` is justified by the type invariant). `sha256(&[u8]) -> [u8; 32]` thin-wraps `sha2::Sha256::digest`. `b64_unpadded` (standard alphabet, no padding — the `hashes.sha256` field per spec) and `b64_url_unpadded` (url-safe, for the event_id suffix) live side-by-side so callers don't pick the wrong one. `redact_for_hash(&mut CanonicalJsonObject)` wraps `ruma_common::canonical_json::redact_in_place` with `RoomVersionRules::V12.redaction` (== V11) and saves/restores `prev_state_events` across the call — the MSC4242 divergence from the v11/v12 spec keep-list. New workspace deps: `sha2` 0.11, `base64` 0.22.1 (pinned on `neutrino-common`, not the root workspace, since this is the only consumer for now). All five helpers are `pub(crate)` with module-level `#![allow(dead_code)]` until B1 (`content_hash` / `reference_hash` / `event_id_from_hash` / `room_id_from_create`) lands in the next commit — the allowance lifts then. 7 unit tests cover key sort, nested-object sort, FIPS 180-2 sha256("abc") vector, both b64 alphabets (including the URL-safe distinction case `[0xff, 0xff, 0xff]` → `////` vs `____`), prev_state_events preservation across redaction, and the no-op-when-absent case so the wrapper can't synthesise the key.

2026-05-22 (PR 1 — type unification, prep for event-id work): The canonical `Event` struct lives in `neutrino-common` (gained `ruma`/`serde_json`/`thiserror` deps). `StoredEvent` and `StoredPdu` in `neutrino-store` are deleted; the storage trait returns `Event` directly. `Event` gains an `auth_events: Vec<OwnedEventId>` field — server-computed, co-located on the struct but not in `raw` (MSC4242). SQLite gains an `events.auth_events_json TEXT NOT NULL DEFAULT '[]'` column (authoritative storage) plus a denormalised `event_edges.edge_type = 'auth'` (for recursive-CTE traversal when state-res lands on a SQLite-backed provider). The `StateProvider` trait's per-step `auth_event_ids(id) -> Cow<[OwnedEventId]>` is replaced by `auth_chain(seeds: &HashSet<OwnedEventId>) -> HashSet<OwnedEventId>` — returns the whole transitive closure in one call so SQLite impls can collapse the walk to a recursive CTE. `validate::parse_event` signature gains `auth_events: Vec<OwnedEventId>` (caller supplies; server-side `calculate_auth_events` produces them at insert time). All 373+ test fixtures across `neutrino-state` migrated. Decision: `event_edges` is a pure derived materialised view of `events` (prev/prev_state from `events.json` arrays, auth from `events.auth_events_json`) — recoverable via DROP + INSERT … FROM events without data loss. No `depth` or `signatures` anywhere (struct, raw bytes, or DB columns) — Synapse uses depth for backfill; we use `origin_server_ts` instead. Full design rationale in `event-id-design.md`. PR 2 introduces `EventBuilder` + reference-hash computation + CSAPI write-handler migration; `mint_id` removal lives there.

2026-05-22: Phase 5 second-pass review fixes. (1) `StateStore::rooms_with_membership` signature changes: input is now `&BTreeSet<Membership>` (closed enum, no duplicate-string footgun, no bogus values), output is `Vec<(OwnedRoomId, Membership)>`. New `Membership` enum lives in `neutrino-store` with `as_str()` / `from_wire()`. The trait doc is tightened to "exactly one row per matching room" — backed by the (room, user) → single current member-event invariant, so no caller-side dedup is needed. (2) Sliding-sync idempotency retry cache now keys on `(pos, body_hash)` rather than `pos` alone: a retry with the same pos but different `lists`/`subs`/`extensions`/`conn_id`/`txn_id` is re-processed instead of served the stale response. Hash uses `DefaultHasher` over the canonical JSON serialisation; serialisation failure falls back to `0` which trivially misses (safe direction). (3) `is_kick` is self-contained — re-parses `content.membership` from the current member event rather than relying on an implicit "caller already verified leave" precondition. (4) `members` CSAPI handler now returns `500 M_UNKNOWN` on storage failure and `400 M_INVALID_PARAM` on a malformed `room_id` rather than masking both as an empty `chunk`. Default response shape (all memberships) confirmed against spec. (5) `SyncError::BadRequest` maps to `M_INVALID_PARAM` (was `M_BAD_JSON`); the e2e test renamed accordingly. (6) Long-poll loop's break condition pulled into a single `wait_for_data = !initial_sync && !timeout.is_zero()` boolean so the body reads "build → break unless still waiting → wait". (7) Test helpers in `sliding_sync::tests` deduplicated into a single `build_stored_event` lower-level constructor shared by `make_event` and `make_event_from_json`.

2026-05-26: Reverses the 2026-05-13 decision to add `EventStore::get_client_txn` / `record_client_txn` for CSAPI txnId deduplication. Neutrino is embedded into the Matrix client (Element X) and the CSAPI is reached over loopback — there is no network between the client's transaction-retry layer and this server, so the cross-restart dedup the methods bought is solving a problem that doesn't exist in the embedded topology. The two trait methods are dropped from `EventStore` in `neutrino-store`, both `SqliteStore` impls and the `client_txns` table from `crates/neutrino-store-sqlite/src/schema.sql` are removed, and the six in-crate tests covering them (E12–E16, E31) and the `record_client_txn` / `get_client_txn` lines inside the `restart_preserves_all_state` fat test are deleted. No handler in `neutrino-http`/`neutrino-main`/`neutrino-ffi`/`neutrino` calls these methods today, so the change is purely a storage-trait excision — the CSAPI `put_event` path is unaffected. `crates/neutrino-store/src/lib.rs` keeps the `UserId` ruma import (still used by `joined_rooms` / `invited_rooms` / `rooms_with_membership` / `joined_members`); `crates/neutrino-store-sqlite/src/store/events.rs` drops the now-unused `UserId` (top-level) and `BOB_USER_ID` (test-mod) imports. Design docs `2026-05-14-sqlite-storage-backend.md` and `2026-05-18-read-write-pool-split.md` still reference `client_txns` but are dated snapshots and left as-is (already partially superseded). Schema is still v1 — no migration framework yet, and there's no live data to migrate.

2026-05-26: Legacy `/sync` → sliding-sync stub. `GET /_matrix/client/v3/sync` lives in `crates/neutrino-http/src/legacy_sync/`. Designed in `docs/legacy-sync-stub.md`. Pure translation helpers in `translate.rs` (32 unit tests) compose the v3 query string into a v5::Request, call into the existing `sliding_sync::handle` (no sliding-sync changes), and reshape the v5::Response into a v3 envelope. Six e2e tests in `tests/e2e_legacy_sync.rs` exercise the live router via `tower::ServiceExt::oneshot`; each test calls `neutrino_http::router(config)` whose `AppState::new` opens a file-backed `SqliteStore` on a `tempfile::NamedTempFile` (mirroring `tests/e2e_sliding_sync.rs`, not the in-memory variant of `SqliteStore`). Key decisions: (a) `required_state` uses the wildcard `(StateEventType::from("*"), "*")` — verified against `sliding_sync/tests.rs::required_state_wildcard_*` which already exercise the form through ruma's v5 types and `build::required_state_matches`. (b) MSC4222 dual-emission: every joined/leave room shape carries both `state.events` and `org.matrix.msc4222.state_after.events` with identical contents (the recommended path; cheaper than the "emit only on opt-in" alternative and lossless). `/versions` advertises `org.matrix.msc4222: true` alongside `org.matrix.simplified_msc3575: true`. (c) Knock rooms are emitted under `rooms.knock` (beyond the design doc's join/invite/leave-only enumeration) because the upstream `candidate_rooms` includes `Membership::Knock` (`sliding_sync/build.rs:292,311`) and skipping them would silently drop rooms the v5 handler chose to return. `knock_state.events` is built by manually stripping each `Raw<AnySyncStateEvent>` from v5's `required_state` down to `{type, state_key, sender, content}` — ruma 0.15 has no `JsonCastable` impl from the type-erased `AnySyncStateEvent` enum to `AnyStrippedStateEvent`, and no general `fn strip()`; `Raw::cast_unchecked` would emit the full event JSON under a stripped type label (soft lie) so manual reshape in `translate::strip_state_event` is the honest path. (d) Conn registry uses `conn_id = "__legacy__"` so legacy and real sliding-sync sessions never collide on `(user_id, conn_id)`. (e) `parse_legacy_query` maps empty `since=` to None (initial-sync semantics matching Synapse); `is_truthy` accepts only `"true"` case-insensitively (Synapse-style permissive `"1"`/`"yes"` parsing deferred). (f) Unknown-membership rooms in `translate_response` probe `room.invite_state.is_some()` as a race-tiebreaker before defaulting to the `join` bucket — handles the narrow window where a fresh invite arrives between `rooms_with_membership` snapshot and the v5 handler emitting it. (g) `prev_batch` is the empty string for joined/leave rooms (no `/messages` impl yet — the token is never redeemed); per-room `summary` / `unread_notifications` intentionally omitted (out of scope, matches the doc). (h) The handler `legacy_sync::handle` mirrors the sliding-sync wrapper's state-extraction (`lock_app` scoped to a block so the std-mutex guard drops before any `.await`) and error mapping (`UnknownPos` → 400 M_UNKNOWN_POS, `BadRequest` → 400 M_INVALID_PARAM, `Storage` → 500 M_UNKNOWN). `fetch_memberships` snapshots `StateStore::rooms_with_membership` for all five `Membership` variants *before* the v5 call so bucketing reflects the same point-in-time the v5 response observes. Not in scope (deferred per `docs/legacy-sync-stub.md`): `creation_content` merge in `create_room`, `device_id` echo in `post_login`, `GET /rooms/{id}/state/{type}/{key}`, rejecting non-v12 `room_version` (the doc's "three cheap-win fixes"). Complement allowlist growth not yet realised.

2026-05-27: Phase 4c — mainline + IAC pass 2 + unconflicted merge → `resolve_state`. Six pure functions in `state_res.rs`: `is_power_event`, `split_power_events`, `mainline`, `mainline_position`, `mainline_sort`, `resolve_state`. The power-event predicate is `m.room.create` || `m.room.join_rules` (sk "") || `m.room.power_levels` (sk "") || (`m.room.member` && sender != state_key && membership ∈ {leave, ban}). **v12 includes m.room.create as a power event** — divergence from synapse v2's predicate, encodes that creators are a power source under v12. Mainline walks PL.auth_events single-step (no transitive walk; under MSC4242 the previous PL is always in the direct set). `mainline_position` walks event.auth_events looking for the closest mainline PL ancestor; no match → returns mainline_len so the event sorts last (synapse parity for `_get_event_mainline_rank`). `resolve_state` glues phase-4a/4b primitives in spec order: separate → full_conflicted (auth_diff ∪ subgraph ∪ conflicted_values) → split power/non-power → reverse-topo sort + IAC pass 1 (empty initial, v2.1 divergence) → mainline_sort anchored on pass-1's resolved PL → IAC pass 2 seeded with pass-1's result → overlay unconflicted (disjoint by construction; unconflicted wins on the unlikely-collision path per spec step 7). 25 case tests + 2 properties. 507 workspace tests passing, 0 failing. fmt + clippy clean.

2026-05-27: Phase 4b — reverse-topological power sort + iterative auth checks. Three pure functions in `state_res.rs`: `power_of_sender`, `reverse_topological_power_sort`, `iterative_auth_checks`. Power lookup reads create + latest `m.room.power_levels` directly from `event.auth_events` (no transitive walk) — confirmed against v12 spec ("looking at their respective `auth_event`s") and synapse `state/v2.py::_get_power_level_for_sender`. The `power_level_auth_ancestor` trait method from the original `state-machine-notes.md` sketch was **not** added — direct scan of `event.auth_events` subsumes it, because under MSC4242 v12 `calculate_auth_events` puts the governing PL directly in the auth_events set. `AuthContext::new` + `user_power` promoted from private to `pub(crate)` in `auth_rules.rs` so the creator-set + PL parsing stay defined exactly once; `power_of_sender` builds a synthetic 2-entry `StateMap` (create + latest non-rejected PL) from `event.auth_events` and hands it to `AuthContext::new`. The sort is Kahn's with `outdegree(e) = |e.auth_events ∩ events|` and BinaryHeap key `(power, Reverse(ts), Reverse(event_id))` — synapse's `(-power, ts, id)` on a min-heap; popping yields highest-power-first / earliest-ts / lowest-id. IAC builds per-event auth_map from `event.auth_events` (filter pre-rejected) then **overlays** entries from `resolved_state` for each key in `auth_events::auth_event_keys(event)` — the overlay is the actual iterative step (earlier accepted events in this pass override the as-shipped auth_events) and matches synapse `_iterative_auth_checks` exactly. `initial_state` is the caller's job (Phase 4c will pass `StateMap::new()` for v12 IAC pass 1 per the v2.1 divergence; pass 2 passes the previous pass's result). 19 new tests in `state_res::tests`: 7 power_of_sender (creator/non-creator × no-PL/PL/users_default, create-itself, rejected PL skipped), 6 reverse_topo (empty / chain / power tiebreak / ts tiebreak / diamond / out-of-set parents excluded from outdegree), 6 IAC (empty pass / accepted create / pre-rejected skip / rule-4 reject / non-state-accept-leaves-state / missing-auth-event error). 160 + 26 prop tests pass for `neutrino-state`; fmt + clippy `--tests -D warnings` clean for the crate; workspace clippy `--workspace --tests -D warnings` clean. Workspace `cargo test` not run end-to-end because `/tmp` is full on the dev machine (sqlite tests use `tempfile`); no code touched outside `neutrino-state` + the `auth_rules.rs` visibility bumps, so no other crate is at risk.

2026-05-27: `GET /_matrix/client/v3/capabilities` stub added to unblock Complement test parents. Returns `{"capabilities":{"m.room_versions":{"default":"12","available":{"12":"stable"}}}}`. Pragmatic divergence: the wire `room_version` we put in create events is the MSC4242 unstable identifier `org.matrix.msc4242.12` (per 2026-05-22), but `gomatrixserverlib.MustGetRoomVersion` (called inside `TestRoomCreationReportsEventsToMyself`) panics on unknown room versions, so the advertised string is the stable `"12"`. The lie is small: complement clients never round-trip the advertised version through neutrino's `createRoom` (it drops `room_version` entirely and substitutes `ROOM_VERSION_ID`), so no test cares. If we later need to advertise honestly, the upstream complement+gomatrixserverlib would need to learn the unstable identifier. Allowlist additions: `TestRoomCreate/Parallel/Can_/sync_newly_created_room` (no code change) plus the two `TestRoomCreationReportsEventsToMyself/parallel/Room_creation_reports_m.room.{create,member}_to_myself` subtests (unblocked by this stub). Regression test: `crates/neutrino-http/tests/capabilities.rs`. Tests not yet run under Complement — no docker in dev sandbox; CI will be the source of truth.

2026-05-28: Phase 6b — `SqliteStateProvider<'a>` lands in `crates/neutrino-store-sqlite/src/store/state_provider.rs`. **Design**: trait is sync, store is async — provider holds `&'a rusqlite::Connection` and is constructed inside a `run_read` / `run_write` closure on the blocking thread, lifetime tied to the closure scope. `get_event` is a single SELECT through the existing `EventRow` machinery; `auth_chain` is one recursive CTE seeded from `json_each(?)` walking `event_edges WHERE edge_type='auth'` backwards, with a `LEFT JOIN events` to detect dangling parent edges (the FK on `child_event_id` is enforced, but `parent_event_id` has no FK by design — federation backfill may reference unseen parents — so the join is the only miss-detection point). Strict-closure invariant: any unresolved id → `StateResError::MissingEvent`. **Schema**: `events.rejected INTEGER NOT NULL DEFAULT 0 CHECK (rejected IN (0,1))` added in place; `user_version` stays at 1 (no live data, no migration framework yet — per Kegan's call). **`Event.rejected`**: lifted onto `neutrino_common::Event` as a server-computed co-located field (same pattern as `auth_events`); `EventInfo` wrapper deleted from `neutrino-state::provider`; `StateProvider::get_event` is now `Option<Arc<Event>>` directly. ~30 sites across `state_res.rs` / `room_core.rs` / `validate.rs` / `tests/prop.rs` rewrote `info.event` → `info` and `info.rejected` → `event.rejected`. **`StateResError`** grew an `Internal(String)` variant so SQL/driver/hydration faults surface without panic — in-memory providers never produce it. **Dependency add**: `neutrino-store-sqlite` takes a path dep on `neutrino-state` (approved). 9 unit tests on `state_provider.rs`: get_event hit / miss / rejected-roundtrip; auth_chain empty / single-seed-no-parents / linear-chain / diamond-dedup / missing-seed / dangling-parent-edge (raw SQL injection — the missing-FK case). Out of scope, deferred to the storage-side `Effect` processor: wiring `RoomCore::apply` against `SqliteStateProvider` inside an `interact` closure, atomic apply+persist transaction, any new top-level method on a store trait. Full workspace `cargo fmt` + `cargo clippy --workspace --all-targets -D warnings` + `cargo test` all clean.

2026-05-28: `validate::parse_event` split into 1a (wire-format only) and a new Phase 1b `validate::validate_pdu(&Event) -> Result<(), FormatError>` carrying the semantic-but-stateless rules — count limits (`MAX_PREV_EVENTS` / `MAX_PREV_STATE_EVENTS`), v12 rule 1.1 (create has no `prev_events`), MSC4242 (create has no `prev_state_events`), v12 rule 9 (`@`-prefixed state_key matches sender, excluding `m.room.member`), and the per-type content checks (`check_create` / `check_member` / `check_power_levels`). What stays in parse_event: required-field presence, JSON type checks, ID parsing, plus the wire-only `AuthEventsPresent` (MSC4242) and `CreateHasRoomId` (v12 rule 1.2 — wire presence signal lost once `Event.room_id` is derived) checks. **Rationale**: `RoomCore::apply` previously presupposed `parse_event` had run all of these — a hand-constructed `Event` could bypass the semantic rules and slip into the auth pipeline. Now `apply` calls `validate_pdu` defensively (alongside `validate_references` — renumbered to 1c). `EventBuilder::build` and `Event::from_wire` also call validate_pdu after parse_event so the wire/build paths still fail fast at the source. `validate_pdu` re-parses `Event.content` from `Box<RawValue>` (one `serde_json::from_str` per call) so it stays correct on a hand-constructed `Event` with non-object content. **Behavioural change worth noting**: an `m.room.create` with malformed entries in `prev_state_events` (e.g. `["not-an-id"]`) now rejects with `MalformedId` (from `parse_event_id_array`) instead of the old `CreateHasPrevStateEvents` — both reject paths, different variant. No tests broke. fmt + clippy --workspace --all-targets -D warnings clean; full suite 0 failures.

2026-05-27: `Event` → ruma `Raw<…>` client-view conversions in new module `neutrino-common::event_view`. Designed in `docs/event-view-conversions.md`. Fixes the CI failure `TestRoomCreate/Parallel/Can_/sync_newly_created_room`: root cause was that `sliding_sync/build.rs` shipped `Event.raw` (canonical v12 / MSC4242 wire bytes, which by design lack `event_id` and — for `m.room.create` — `room_id`) straight through to clients, so complement's `SendEventSynced` could never match the `event_id` that `PUT /send` had just returned. Four trait impls land in `event_view`: `From<&Event> for Raw<AnySyncTimelineEvent>` and `From<&Event> for Raw<AnyTimelineEvent>` (infallible — timeline targets accept both message and state events); `TryFrom<&Event> for Raw<AnySyncStateEvent>` and `TryFrom<&Event> for Raw<AnyStrippedStateEvent>` (fallible — state-shaped targets require `state_key`, and `Event.state_key` is `Option<String>`). Plus the free function `strip_state_event(&Raw<AnySyncStateEvent>) -> Raw<AnyStrippedStateEvent>` — orphan-rule forbids it as a trait impl since both `Raw<…>` types live in ruma. The function is infallible: `Raw::from_json` / `Raw::cast_unchecked` don't validate against the type parameter (validation happens only at `.deserialize()` time), so we can't rely on the input being a state event — but "collect whichever of `type` / `state_key` / `sender` / `content` exist at the root" is well-defined for any JSON value (`Value::get(&str)` returns `None` on non-objects, so the loop is a no-op). Empty `{}` is the well-defined result for non-object or empty input; structural validity surfaces at the caller's downstream `.deserialize::<AnyStrippedStateEvent>()`, not here. The single-variant error type `StateEventConversionError::NotAStateEvent` is reached only by the `TryFrom<&Event>` impls when `Event.state_key` is `None`; it's `#[from]`-bridged into `SyncError::EventConversion` and mapped to HTTP 500 `M_UNKNOWN` in both `lib.rs::sync` and `legacy_sync::handle`. Shared helper `enrich_for_client(&Event, IncludeRoomId)` parses `Event.raw` once, merges `event_id` (always) and `room_id` (per policy: `OnlyOnCreate` strips it from non-create timeline events Synapse-style, `Always` for the future `/event/{eventId}` endpoint). Federation paths (`StorageBackend::persist_event` → outbox) untouched — they keep shipping `Event.raw` verbatim so federation peers can still verify the reference hash. Migration: `sliding_sync/build.rs` timeline + required_state sites become `e.iter().map(Into::into)` / `.map(TryInto::try_into).collect::<Result<_, _>>()?`; invite member event uses `ev.try_into()?` — a storage-layer invariant violation (state row with `state_key = NULL`) surfaces as 500 rather than panicking the handler (CLAUDE.md bans `.expect` there). The local `strip_state_event(&Event)` helper deleted. `legacy_sync/translate.rs::knock_room_shape` collects `Vec<Raw<AnyStrippedStateEvent>>` and hands it straight to `json!` — one parse per event, not four round-trips; the local `strip_state_event(&Raw<…>)` helper deleted along with its two unit tests (`strip_state_event_keeps_only_canonical_four_fields` had an equivalent in `event_view::tests`; `strip_state_event_handles_partial_events` was permissive about partial inputs in the same way the new function is). The `events` feature added to the `ruma` dep of `neutrino-common` (was implicitly enabled transitively via `neutrino-http`'s `client-api-s` feature; needed directly now for the `Raw<AnyXxx>` types). Tests: 13 unit tests in `event_view::tests` (including `from_event_for_sync_timeline_preserves_unsigned` and `try_from_event_for_sync_state_preserves_unsigned_and_prev_content` pinning that `unsigned` / `prev_content` survive the enrichment pipeline, plus `strip_state_event_returns_empty_for_non_object_input` covering the degenerate `Raw` cases); `build_output_raw_lacks_event_id` in `neutrino-state::event_id::tests` pins the load-bearing invariant that `EventBuilder` never serialises `event_id` into `raw` for either create or non-create events; 3 e2e regressions (one per missing-field case) that fail RED against the wire-bytes passthrough — verified by reverting both sliding_sync sites and observing each regression panic on missing `event_id` / `room_id`. Full workspace: cargo fmt + clippy --workspace --all-targets -D warnings + cargo test --workspace all clean; 470 tests pass / 0 fail / 0 ignored.

2026-05-28: `DagStore::missing_events` contract realigned with Synapse's `_get_missing_events` (`storage/databases/main/event_federation.py`). Two semantic changes, both reversing parts of the 2026-05-13 strict-validation contract. (1) Unknown event IDs in `latest` / `earliest` are no longer rejected with `InvalidInput` — unknown `latest` IDs contribute no parents to the BFS frontier (empty `event_edges` row → empty expansion), unknown `earliest` IDs are no-ops on the walk (no walked event matches). The strict-existence-check was a 2026-05-13 invention that contradicts the federation use case: peers legitimately hold events we haven't seen yet, and an `InvalidInput` here would turn every routine federation gap-fill into a 400. (2) `latest_events` themselves are now excluded from the result — they are the *boundary* the requester already has, not part of the answer. Initial BFS frontier is parents-of-`latest` (not `latest` itself), and the `excluded` set passed to `walk_prev_events` is `earliest ∪ latest` (so a path that loops back into a sibling `latest` stops cleanly). This matches Synapse exactly and matches the design intent in `docs/get-missing-events.md`. `events_before` is unchanged — strict existence check still runs, since CSAPI callers pass IDs they just got from the store and any unknown ID there indicates caller bug, not federation lag. Storage-layer split: monolithic `validate_inputs` becomes two functions — `validate_room_exists` (called by both methods) and `validate_events_exist` (only `events_before`); the latter's IN-clause-chunk constant `VALIDATE_INPUTS_CHUNK` renamed to `VALIDATE_EVENTS_EXIST_CHUNK` to track the rename. Tests rewritten for the new semantics: D7 (excludes_earliest) now expects only `b`, not `[c, b]`; D11 (cross-room via edges) expects `missing_events` empty rather than `[a]` (a's only parent is cross-room `b`, rejected by `hydrate_pdu`); D17 (multi-seed `latest`) asserts cardinality + set membership not specific order (per-seed BFS interleaving is implementation detail, not contract); D21 / D22 invert from `InvalidInput` to empty-result-on-unknown. Three new tests: D23a (50-leaf wide fan-out — guards against frontier truncation), D23b (limit=1 boundary), D23c (Synapse's `_setup_room_for_backfill_tests` 13-event DAG, asserting 12-event unbounded walk and 5-event walk with `earliest=[3]`). `DagStore::missing_events` trait doc rewritten to document the new contract; `DagStore::events_before` doc unchanged. `docs/get-missing-events.md` (working note, untracked) is now landed at the storage layer — federation handler (`POST /_matrix/federation/v1/get_missing_events/{roomId}`) is still TBD per the design doc.

2026-05-28: `POST /_matrix/federation/v1/get_missing_events/{roomId}` — first inbound federation endpoint lands. Architecture follows the design doc `docs/get-missing-events.md`: new `crates/neutrino-http/src/federation/` submodule sibling to `sliding_sync/` and `legacy_sync/`, containing `mod.rs` (`FedError` enum + `IntoResponse`) and `get_missing_events.rs` (the handler). `FedError` is the federation analogue of `SyncError`: `BadRequest(&'static str)` → 400 `M_INVALID_PARAM`, `RoomNotFound` → 404 `M_NOT_FOUND`, `Storage(#[from] StorageError)` → 500 `M_UNKNOWN`. Eight handler-side decisions worth pinning. (1) **Wire bytes verbatim.** Response `events` is `Vec<Box<RawJsonValue>>` populated from `Event.raw` directly — no `event_view` enrichment, no `event_id` / `room_id` injection. Federation peers must be able to recompute the v12 / MSC4242 reference hash bit-for-bit. Pinned by the `wire_bytes_passthrough` e2e test. (2) **`min_depth` parsed-then-ignored.** Field is `_min_depth: Option<i64>` with `#[serde(rename = "min_depth")]` so malformed JSON (e.g. `min_depth: "abc"`) still produces a 400. The rename was a deviation from the F3 fix-up prescription that proposed just renaming to `_min_depth` — without the rename, serde would silently drop the wire field, the "parsed for spec compliance" rationale would be a lie, and the `min_depth_field_ignored` test premise would collapse. `i64` (not `u64`) because the spec defines `depth` as a signed integer; `u64` would reject negative values at serde and contradict the accept-but-ignore intent. Doc comment on the field documents the rename and the signed-int type choice. (3) **Limit saturates.** Default 10, cap 20 — clamping (`u32 as usize`, then `.min(MAX_LIMIT)`) not a 400. Spec text "MUST return no more than 20" is a hard ceiling, not a 400 trigger; saturating matches Synapse. (4) **`Path<String>` + manual `OwnedRoomId::try_from`** rather than `Path<OwnedRoomId>`. The path extractor's default 400 is plain-text; clients of Matrix federation expect `{errcode, error}` JSON. Matching the precedent of `members` in `lib.rs`. Pinned by the `malformed_room_id_returns_400_with_errcode` e2e test. (5) **Hand-rolled `RequestBody` / `ResponseBody`** serde mirrors instead of `ruma::api::federation::event::get_missing_events::v1::{Request, Response}` — ruma's `#[request]` / `#[response]` macros don't derive plain `Serialize` / `Deserialize`. Same pattern as the existing `SyncRequestBody` / `SyncResponseWire` in `lib.rs`. (6) **`federation-api-s` ruma feature dropped.** The design doc prescribed adding it, but with the hand-rolled mirrors there's no actual `use ruma::api::federation::...` import — the feature would pull `ruma-federation-api` + `ruma-signatures` for zero code. Dropped. If a future federation endpoint wants the ruma server-side types directly, re-add. (7) **`#[cfg(test)] pub(crate) fn router_with_store(config, store, tempfile) -> Router`** test-only constructor added to `lib.rs`. Tests live in `src/federation/tests.rs` (same crate) so `pub(crate)` suffices; `#[cfg(test)]` gates the constructor from production builds. Used by the seven e2e tests that need a real BFS-walkable DAG (`create ← join ← msg[0] ← … ← msg[n-1]` chain via `EventBuilder` with explicit `prev_events`). The other five tests (bad-request paths, unknown-room, latest-not-in-room) seed via the existing CSAPI `/createRoom` flow because they don't need a non-flat DAG. (8) **`router()` refactored to share `build_router(config, state)`** so the production constructor and the test-only `router_with_store` mount byte-identical routing tables. Tests: 12 e2e tests in `crates/neutrino-http/src/federation/tests.rs` (B1–B12, mapping to every row of design doc §B plus malformed-`room_id`). Storage-layer edge cases (cycle, missing parent, wide fan-out, limit=1, earliest-boundary-exclusive, unknown-earliest, unknown-latest) covered by the existing `dag.rs` tests landed 2026-05-28 morning. The two candidate Complement tests (`TestInboundCanReturnMissingEvents`, `TestGetMissingEventsGapFilling`) remain blocked on state-res (Phase 4c/6) per `complement/VIABLE-TESTS.md`. Workspace: 538 tests pass, fmt + clippy `--workspace --tests -D warnings` clean.

2026-05-29: Review finding I2 — `/get_missing_events` returned 500 (not the spec-required 404) for an unknown room, because `missing_events` maps unknown-room to `StorageError::InvalidInput` and the handler had to pre-check via `get_room_version(...).is_none()`. Considered adding a `StorageError::NotFound` variant fanned to 404 in `FedError` (which would also delete the pre-check), but rejected it: widening a foundational cross-crate error type consumed by every storage caller is disproportionate to one endpoint's 404, and overloading the shared error's semantics is heavier than the problem. Instead added `RoomStore::room_exists(room_id) -> Result<bool, StorageError>` as a default trait method (`Ok(self.get_room_version(room_id).await?.is_some())`); `SqliteStore` inherits it unchanged (a backend with a cheaper `SELECT 1` probe may override). The handler now pre-checks `if !store.room_exists(&room_id).await? { RoomNotFound }` — intention-revealing, and reusable by future room-scoped federation/CSAPI endpoints. Accepted trade-off: the room is still looked up twice per request (the `room_exists` pre-check, then `validate_room_exists` inside `missing_events`) — negligible on an embedded single-user SQLite, and it keeps `StorageError` / `FedError` / `validate_room_exists` / the `dag.rs` D18+D20 tests untouched. B4 (`unknown_room_returns_404`) is the unchanged regression guard; full workspace test suite + clippy + fmt clean.

2026-05-29: Acted on review findings I1/I3/I4/I8 (+ test coverage I5/I6) on `/get_missing_events`; I7 shelved, I9 left as a suggestion. **I1 (doc):** the `MAX_LIMIT = 20` comment quoted a spec sentence ("This endpoint MUST return no more than 20 events…") that does *not* exist in v1.18 — verified against the spec, where `limit` is documented only as "Defaults to 10" with no maximum. Rewrote the comment to attribute the cap to Synapse's `min(limit, 20)` + per-request work bounding; behaviour unchanged (the cap stays — it matches the reference server, only the false citation was the defect). **I3:** `earliest_events` is Required per spec, so dropped its `#[serde(default)]` — a body omitting it now 400s instead of being silently treated as empty. **I4:** the BFS yields newest-first; the handler now `.rev()`s to topological oldest-first order before serialising, matching Synapse (which reverses its walk before responding). Done at the handler rather than in `DagStore` because ordering is a federation-protocol concern and `events_before`'s documented reverse-chronological contract must stay intact. **I8:** removed the redundant `#[serde(default)]` from `limit`/`_min_depth` (`Option` fields already deserialize a missing key to `None`; only the `Vec` fields need it). **I7** (double parse — `Json<Value>` then `serde_json::from_value`) left as-is: collapsing to `Json<RequestBody>` while preserving the 400-on-any-rejection mapping wants a reusable matrix-specific axum extractor / rejection layer, deferred until a second federation endpoint needs it. **I9** (inconsistent 400 detail strings) not implemented — suggestion only. Tests (7 new e2e, B13–B19): B13 oldest-first ordering (asserts a *sequence*, so a newest-first regression fails), B14 earliest *message* boundary exclusion (closes B5's body-filter blind spot — B5's `earliest` was the bodyless create event), B15/B16/B17 malformed `min_depth` / event-id / content-type → 400, B18 explicit `limit:0` → empty (Synapse parity), B19 missing `earliest_events` → 400. 545 workspace tests pass; clippy `--workspace --all-targets -D warnings` + fmt clean.

2026-05-29: Removed the dead `crates/neutrino-sqlite` pre-trait stub. It was scaffolding from before the `StorageBackend` trait existed — a bare `Store` over raw `rusqlite` with every method `.unwrap()`-ing and its own `schema/events.sql` — superseded on 2026-05-21 by `neutrino-store-sqlite` (`SqliteStore`, the real trait impl). The 2026-05-21 merge note already recorded it was "no longer a dependency of `neutrino-http`", and nothing has depended on it since: confirmed no crate lists it as a Cargo dependency and no Rust code references `neutrino_sqlite` apart from the tracing target in `neutrino-main::platform::DEFAULT_FILTER` (removed) and a module bullet in README.md (removed). The workspace `members = ["./crates/*"]` glob drops it automatically once the directory is gone; `Cargo.lock` regenerated. The removed crate carried no tests of its own, so this is a pure excision: `cargo test --workspace` green (535 pass / 0 fail / 0 ignored), clippy `--workspace --all-targets -D warnings` + fmt clean.

2026-05-29: **PR — `GET /_matrix/federation/v1/backfill/{roomId}`** (the last federation endpoint, sibling to `get_missing_events`). Net decisions made across the PR, all with Skye over Matrix: (1) **Storage reuse, no trait change.** Backfill = "walk `prev_events` back from the `v` seeds, seeds included, newest-first", which is exactly `DagStore::events_before`'s contract — so the endpoint is HTTP-layer-only and the frozen `StorageBackend` trait is untouched. (2) **Ordering is a priority queue keyed on `origin_server_ts`, not `depth`.** `walk_prev_events` (shared by `events_before` + `missing_events`) is a `BinaryHeap` max-priority-queue popping highest `origin_server_ts` first, ties broken by ascending `event_id` — mimicking Synapse's depth-descending backfill walk (`get_backfill_events` sorts by `(-depth, -stream_ordering)`). The key is `origin_server_ts` because this server deliberately stores no `depth` (`neutrino-state/src/validate.rs` parses-and-ignores it, using `origin_server_ts` "for everything Synapse would use depth for, backfill ordering included"); a real `depth` column was out of scope (would contradict that decision + need a migration). This replaced an earlier FIFO `VecDeque` BFS that walked breadth-first *per seed* and so, on the common multi-seed/forked backfill, interleaved by BFS level rather than recency and could return an older event ahead of a newer one under a `limit`; the heap merges all branches into one global newest-first stream so `limit` yields the genuinely newest N. The `events_before`/`missing_events` trait contract is tightened to "newest `origin_server_ts` first, ties by ascending `event_id`". (3) **Newest-first, no `.rev()`** — `events_before` yields reverse-chronological and Synapse's backfill returns the same; the sibling's oldest-first `.rev()` is a `get_missing_events`-specific choice, not copied. (4) **Transaction envelope** `{origin, origin_server_ts, pdus}` — hand-rolled `ResponseBody` (ruma's `#[response]` emits no plain `Serialize`); `origin` = `config.server_name`, `origin_server_ts` from inline `SystemTime`; `Event.raw` wire bytes verbatim so each reference hash round-trips on the peer. (5) **Unknown / cross-room `v` skipped, not 500'd** — `events_before` rejects an unheld seed with `InvalidInput` (→ 500), but a peer may legitimately backfill from an event we've never seen, so the handler pre-filters `v` through `EventStore::get_events` (silently omits absent ids) and room-scopes the survivors. Matches Synapse, which only seeds from events it has; costs one extra read round-trip, negligible on embedded SQLite. (6) **Repeated `?v=` via a hand-rolled `RawQuery` parser** — axum 0.8's `Query<HashMap>` collapses repeated keys and `serde_html_form`/`axum-extra` are only transitive deps (a direct dep needs sign-off), so `parse_backfill_query` + `percent_decode` walk `RawQuery` directly. Percent-decoding is load-bearing (ruma sends `$`→`%24`); `percent_decode` *rejects* any malformed escape — non-hex **or** incomplete (trailing `%`, `%`+one char) — returning `None`. Empty/blank `v` (`?v=`, bare `?v`) is dropped at parse time. (7) **Limit** — optional; a *missing* or unparseable limit defaults to 10 and a positive value saturates at 100 (`min(limit, 100)`; spec sets no max), trusted-mesh leniency. An *explicit* `limit=0` is **400**, matching Synapse's backfill servlet (`if not limit: return 400`). Defaulting on *missing* is the one deliberate divergence from Synapse (which 400s on missing too), kept for the single-user mesh. (8) **Other errors** — empty/blank `v` → 400 `M_INVALID_PARAM`; unknown room → 404 (pre-checked via `room_exists`). (9) **Trusted-mesh deviations** carried over from `get_missing_events`: no X-Matrix auth, no signature verification, no history-visibility/redaction filtering — this endpoint only reads (we're the responder; `persist_historical_event` is for the future outbound direction). Tests: 9 unit (query parser/decoder) + 10 e2e (BF1–BF10) on the endpoint, plus the rewritten DAG ordering tests (D2/D4/D12/D13/D14 assert newest-first-by-ts; new D12b proves a multi-seed `limit` returns the globally-newest events). Candidate Complement tests (`TestBackfillingHistory`) remain state-res-blocked (Phase 6) per `complement/VIABLE-TESTS.md`. Full workspace `cargo test` green (597), clippy `--workspace --all-targets -D warnings` + fmt clean.

2026-06-01: Testing-only multi-user identity shim added behind a `multi-user-shim` cargo feature (design: `docs/superpowers/specs/2026-06-01-multi-user-shim-design.md`; plan: `docs/superpowers/plans/2026-06-01-multi-user-shim.md`). Feature defined on `neutrino-http`, passed through `neutrino-main` + the `neutrino` binary; `neutrino-ffi`/Android never enables it, the Complement Dockerfile + a dedicated CI lane do. An `AuthUser(OwnedUserId)` axum extractor resolves `Authorization: Bearer <token>` against an in-memory `user_tokens: Arc<Mutex<HashMap<token, OwnedUserId>>>` on `App` — feature-ON returns 401 `M_MISSING_TOKEN`/`M_UNKNOWN_TOKEN` on miss, feature-OFF returns `config.user_id()` so the single-user path is byte-identical (handler signatures are identical in both states; the `#[cfg]` lives inside the extractor impl + the `multi_user` module). `/register` (two-step UIA stub) and `/login` mint a fresh `syt_<32 alnum>` token per user (token→user only; a new login = a new token). Threaded through createRoom, send/state, sliding + legacy sync; `/profile` + `/account_data` routes de-hardcoded from baked-in `@alice` to `{user_id}` params. **Test policy (decided during execution):** because auth is mandatory feature-ON, the pre-existing suite — which sends no token — only passes feature-OFF, which stays the full regression contract; feature-ON CI runs ONLY the shim's own tests (`cargo test -p neutrino-http --features multi-user-shim --lib multi_user` + `--test e2e_multi_user`), not the whole suite. Retrofitting tokens into every legacy test was rejected as non-surgical. **Verification** is per-user-createRoom isolation (alice's and bob's tokens each create a room; each user's sync sees only their own) because createRoom does NOT honour the `invite` list — cross-user visibility waits on the membership follow-up (`/join`, `/invite`, `/leave`). Shared-secret `/_synapse/admin/v1/register` and per-device token scoping deliberately excluded. Review fix: `user_tokens` is `Arc<Mutex<…>>` (not a bare `Mutex` field) so the extractor clones the Arc under a brief lock instead of holding the coarse `App` lock across resolution, matching the `sync_state`/`room_registry` pattern. fmt + clippy `--workspace --all-targets -D warnings` (feature-off) + clippy feature-on + `cargo test --workspace` (feature-off) + the feature-on shim tests all clean. `.git` was read-only in the execution environment, so the work is staged in the working tree (not committed).

2026-06-01: **Multi-user-shim code review (Skye, over Matrix) — fixes for findings #1, #3-#5.** (1) **`/login` dead-token (major).** Feature-on `/login` returned `200` + the hardcoded `syt_1234567890abcdef` on a `provision` failure, but that constant is never inserted into `user_tokens`, so the "successful" login handed back a token that 401'd `M_UNKNOWN_TOKEN` on the next authed request — asymmetric with `/register`, which 400s. Decision: make `/login` mirror `/register` (return `400 M_INVALID_USERNAME`) rather than the alternative of falling back to the default user and minting a real token; surfacing the error is clearer than silently re-homing a bad login, and Complement only ever sends valid localparts so the path is unreachable in practice. Return type widened to `(StatusCode, Json<Value>)`. (2) **`/register` localpart normalisation (nit).** `username` now goes through `localpart_of` so a full MXID is accepted, matching `/login`. (3) **Stateless UIA (minor).** Documented in a code comment that the shim keeps no per-session state and relies on the client resending `username` on the auth-completion request (true for Complement); a real UIA session store was judged out of scope for a test shim. (4) **"byte-identical feature-off" (minor).** The profile/account-data routes are de-hardcoded to `{user_id}` unconditionally (intentional, §5), which technically widens those two routes feature-off (200 vs prior 404 for a non-default user). Decision: qualify the design-doc goal to document the exception rather than feature-gate the routes (gating would re-introduce `config` into `build_router`, undoing this PR's simplification, for a change with no production impact — the embedded client only queries its own user). 2 regression e2e tests added for #1.

2026-06-02: **Client-Server membership endpoints** (`/join`, `/leave`, `/invite`, `/kick`, `/ban`, `/unban`) + `/createRoom` invite-list, implemented then Synapse-aligned over a Matrix review with Skye (folds three prior entries). Six thin handlers in `crates/neutrino-http/src/membership.rs` emit one `m.room.member` event through the per-room actor (`RoomRegistry::send_event`) — no change to the actor, `RoomCore`, auth rules, or storage trait; auth (rule 5) + state-res happen there. Key decisions: (1) `/kick` and `/unban` both emit `membership: leave`, and the auth rules pick the kick-vs-unban arm from the target's *current* membership — so `/unban` pre-checks the target is `ban` and else returns **403 `M_BAD_STATE`** (Synapse parity, `room_member.py:1000-1006`) rather than silently kicking a joined member. (2) `/leave` and `/unban` return **404 `M_NOT_FOUND`** for a room that was never created (`require_room` → `RoomStore::room_exists`), matching Synapse (`:1135-1152`; unban hits it because Synapse maps unban→leave at `:842`); for an *existing* room `/leave` is a 200 no-op unless the caller is `invite`/`join`/`knock` (spec; avoids a spurious rule-5.5.1 self-leave 403). (3) All four targeted endpoints propagate optional `reason`; bodies are `Option<Json<Value>>` so a bodyless POST doesn't 415. (4) Shim-incompatible tests (tokenless CSAPI seeding) are **gated off** `multi-user-shim` rather than tokenised — per-fn `#[cfg(not(...))]` on the 3 `federation::tests` bad-request cases, file-level on `tests/e2e_legacy_sync.rs` + `tests/e2e_sliding_sync.rs` — since the shim exists for multi-user attribution (its own `--test e2e_multi_user` target) and these are single-user edge-case suites. Also corrected 3 design-doc nits (it had claimed `/unban` drops `reason`, `room_actor_response` went `pub(crate)`, and handlers take `Json<Value>`). Deferred: `/forget`, join-by-alias, displayname/avatar carry-over, remote delivery, Synapse/Complement test ports. e2e in `tests/e2e_multi_user.rs`; whole-crate `cargo test -p neutrino-http` + clippy `-D warnings` + fmt green in both feature states.

2026-06-02: Global join endpoint `POST /_matrix/client/v3/join/{roomIdOrAlias}` + two review fixes (Skye over Matrix). Complement's `JoinRoom`/`MustJoinRoom` hit this *global* path (`client/client.go:236-239`), not the room-scoped `/rooms/{id}/join` — so the membership work hadn't actually unblocked the suite. Added `membership::join_by_id_or_alias`, a thin wrapper wired alongside the room-scoped route. Decisions: (1) **room ids only** — a syntactically valid alias (`#…`) returns `404 M_NOT_FOUND` "No such room alias" (Synapse parity; we have no room directory, so every alias is unresolvable), detected via `RoomAliasId::parse` before delegating; a string that is neither a valid id nor a valid alias falls through to `join`'s `400`; `server_name` is accepted-and-ignored (single-server). (2) **idempotent re-join** — `join` (hence the global path) pre-reads the caller's `current_membership` and returns `200 {room_id}` with no event when already `join`, matching Synapse's no-op re-join (`room_member.py:1015-1025`); without it every call stacked a duplicate `m.room.member` (actor idempotency keys on the persisted event-id, and a fresh join has a new id). Only `join` is skipped — `invite`/`leave`/`ban`/absent still emit. Unblocks (pending an empirical Complement run) `msc4222/TestSync`, `TestCumulativeJoinLeaveJoinSync`, and most of the membership suite. 3 e2e tests (`global_join_by_room_id_succeeds`, `global_join_by_alias_is_404`, `repeated_global_join_is_idempotent`). fmt + clippy `-D warnings` + `cargo test -p neutrino-http --features multi-user-shim` (21) green in both feature states.

2026-06-02: Complement run on the new membership/join allowlist (Skye over Matrix) — 7 passed as-is, 3 server fixes applied, 2 parked. (1) **Empty-body POST → 400**: a bare `POST …/join` sends a 0-byte body with `Content-Type: application/json`; axum's `Option<Json<T>>` yields `None` only when the content-type is *absent* (`json.rs:116-134`) and otherwise runs serde on zero bytes → 400. Replaced `Option<Json<Value>>` on all six membership handlers with an `OptionalBody` `FromRequest` extractor that maps an empty body to `None` and parses leniently (invalid JSON → 400 `M_NOT_JSON`). Chosen over the raw-`Bytes`-in-each-handler approach (one reusable extractor, not six ad-hoc parses) after Skye flagged the latter as non-canonical. (2) **Kick of a non-member → 200**: a powerful sender's redundant `leave` is auth-valid under v12, so the actor accepted kicking a user who is not in the room. Now `kick` pre-checks the target's current membership and returns `403 M_FORBIDDEN` ("The target user is not in the room") unless it is `join`/`invite`/`knock` — mirroring Synapse `room_member.py:1027-1045`. (3) **`PUT /state/{type}/` (trailing slash, empty key) → 404**: the spec marks the trailing slash optional for an empty state key (`room_state.yaml`; Synapse registers both regexes), and axum treats `…/state/{type}/` as a path distinct from `…/state/{type}`, so added a second route to `put_state_empty_key`. 3 new e2e regressions (`global_join_with_empty_body_succeeds`, `kick_user_not_in_room_is_forbidden`, `put_state_trailing_slash_empty_key_succeeds`). **Parked**: `TestCumulativeJoinLeaveJoinSync` (legacy `/sync` reuses sliding-sync's ephemeral `pos`, so replaying a stale `since` token → 400 `M_UNKNOWN_POS`; needs a durable legacy token, not a one-liner) and `TestMembersLocal/*` (dropped — fixture sets presence, an out-of-scope EDU). Allowlist now carries 12 of the 15. fmt + clippy `-D warnings` + `cargo test -p neutrino-http --features multi-user-shim` (24) green in both feature states.

2026-06-02: Legacy `/sync` recovers a stale/unknown `since` token via a full re-sync instead of 400ing (option A of the Matrix discussion with Skye; unblocks `TestCumulativeJoinLeaveJoinSync`). **The mismatch**: the legacy stub maps `since` → sliding-sync's `pos`, but `pos` is an ephemeral single-cursor per-connection value (`sliding_sync/mod.rs:215` rejects any non-last-issued pos with `UnknownPos`), whereas legacy `since` tokens are durable/replayable — so replaying a token the connection has advanced past returned `400 M_UNKNOWN_POS`. **Fix**: `legacy_sync::handle` catches `UnknownPos` from the first `sliding_sync::handle` and retries once with `pos = None` (a full initial sync returning current state + a fresh token); no change to the sliding-sync connection model. Chosen over B (split `UnknownPos` into stale-vs-malformed, keeping 400 for garbage — extra surface on the shared error enum for a thin distinction) and C (durable stream-position legacy token via `events_after`, the faithful incremental fix but a large refactor away from the thin-veneer design; deferred). **Known limitation**: a stale token collapses to "state now", not a faithful cumulative delta — fine for the embedded single client and for this test (which only asserts current membership), documented in `docs/legacy-sync-stub.md`. Flipped the existing `legacy_sync_bad_since_returns_m_unknown_pos` test (which pinned the inherited-from-v5 400 behaviour) to `legacy_sync_unknown_since_falls_back_to_initial` (asserts 200 + fresh `next_batch`); added `legacy_sync_cumulative_join_leave_join` (single-user join→leave→join, replay the pre-leave token, assert room in `rooms.join` not `rooms.leave`). Re-added `TestCumulativeJoinLeaveJoinSync$` to the allowlist (13 of the original 15 now in; `TestMembersLocal` stays dropped — fixture needs presence). fmt + clippy `-D warnings` + `cargo test -p neutrino-http` (both feature states) green. Still needs a CI Complement run to confirm the Complement test goes green (no local docker).

2026-06-02→03: GET room state (`feat/read-state` PR). `GET /rooms/{room}/state` (bare array of full events), `…/state/{type}/{key}`, `…/state/{type}` (+ trailing slash) — **neutrino-http only**, reading the already-materialised current state via existing `StateStore::current_room_state` / `current_state_event`; full-list + `?format=event` reuse `From<&Event> for Raw<AnyTimelineEvent>` (event_id + room_id enrichment), default single-key returns the event `content`. **Synapse-aligned** (`synapse-main/.../room.py`): `format` validated as the enum `{content,event}` → `400 M_INVALID_PARAM`; missing event → `404 M_NOT_FOUND` "Event not found." (byte-identical). **Intentional divergence** (trusted-embedded, no access control): no `AuthUser`/membership/history-visibility gating — Synapse 403s non-members and returns *state-at-leave* for departed users; we always return current state, matching the unauthenticated `members` read. Built from a written plan (`docs/superpowers/plans/2026-06-02-get-room-state.md`) + the mandated 5-subagent diff review (only findings were test gaps, closed; pre-existing `members` non-enrichment of `event_id`/`room_id` flagged as a follow-up). 6 e2e tests. Allowlisted the GET-state Complement subtests; those entries anchor the `/^rooms$/` path segment because Go's `-run` matches each `/`-split segment **unanchored**, so a bare `rooms` would substring-match (and silently run) the sibling `joined_rooms` subtest — the CI red Skye caught. **Deferred to separate changes**: the trivial `GET /joined_rooms` wire-up (over existing `StateStore::joined_rooms`), and the proper fix of anchoring segments in the allowlist parser (TODO in `complement/allowlist.txt`). fmt + workspace clippy `-D warnings` + workspace test + shim clippy/e2e all green locally; Complement runs in CI (NDK/uniffi compile steps need the Android toolchain, absent in-sandbox).

2026-06-03: Migrated dev tooling from bash (scripts/compile.sh, scripts/complement.sh) to a Rust `cargo xtask` crate, and set up GitHub Packages Maven publishing of the bindings on `v*` tags. Rationale: single source of truth runnable identically locally and in CI (CI is a thin wrapper around `cargo xtask`); tags give immutable, version-pinned releases. xtask is an orchestrator (shells out to cargo/cargo-ndk/gradlew/docker/go/wget/tar) — no download/extract logic reimplemented in Rust. Deps: clap + thiserror (both already workspace deps); no new external crates.
